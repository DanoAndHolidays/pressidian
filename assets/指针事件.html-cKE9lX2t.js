import{_ as n,c as a,b as s,o}from"./app--ELuHcXR.js";const t={};function p(c,e){return o(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="指针事件" tabindex="-1"><a class="header-anchor" href="#指针事件"><span>指针事件</span></a></h1><p>指针事件（Pointer Events）是一种用于处理来自各种输入设备（例如鼠标、触控笔和触摸屏等）的输入信息的现代化解决方案。</p><h2 id="一段简史" tabindex="-1"><a class="header-anchor" href="#一段简史"><span>一段简史</span></a></h2><p>让我们先做一个简短的概览，以便你对指针事件及其在其它事件类型中所处位置有个粗略认识。</p><ul><li><p>很早以前，只存在鼠标事件。</p><p>后来，触屏设备开始普及，尤其是手机和平板电脑。为了使现有的脚本仍能正常工作，它们生成（现在仍生成）鼠标事件。例如，轻触屏幕就会生成 <code>mousedown</code> 事件。因此，触摸设备可以很好地与网页配合使用。</p><p>但是，触摸设备比鼠标具有更多的功能。例如，我们可以同时触控多点（多点触控）。然而，鼠标事件并没有相关属性来处理这种多点触控。</p></li><li><p>因此，引入了触摸事件，例如 <code>touchstart</code>、<code>touchend</code> 和 <code>touchmove</code>，它们具有特定于触摸的属性（这里不再赘述这些特性，因为指针事件更加完善）。</p><p>不过这还是不够完美。因为很多其他输入设备（如触控笔）都有自己的特性。而且同时维护两份分别处理鼠标事件和触摸事件的代码，显得有些笨重了。</p></li><li><p>为了解决这些问题，人们引入了全新的规范「指针事件」。它为各种指针输入设备提供了一套统一的事件。</p></li></ul><p>目前，各大主流浏览器已经支持了 <a href="https://www.w3.org/TR/pointerevents2/" target="_blank" rel="noopener noreferrer">Pointer Events Level 2</a> 标准，版本更新的 <a href="https://w3c.github.io/pointerevents/" target="_blank" rel="noopener noreferrer">Pointer Events Level 3</a> 已经发布，并且大多数情况下与 Pointer Events Level 2 兼容。</p><p>因此，除非你写的代码需要兼容旧版本的浏览器，例如 IE 10 或 Safari 12 或更低的版本，否则无需继续使用鼠标事件或触摸事件 —— 我们可以使用指针事件。</p><p>这样，你的代码就可以在触摸设备和鼠标设备上都能正常工作了。</p><p>话虽如此，指针事件仍然有一些重要的奇怪特性，你应当对它们有所了解以正确使用指针事件，并避免一些意料之外的错误。我们将在本文中对它们进行介绍。</p><hr><h2 id="指针事件类型" tabindex="-1"><a class="header-anchor" href="#指针事件类型"><span>指针事件类型</span></a></h2><p>指针事件的命名方式和鼠标事件类似：</p><table><thead><tr><th>指针事件</th><th>类似的鼠标事件</th></tr></thead><tbody><tr><td><code>pointerdown</code></td><td><code>mousedown</code></td></tr><tr><td><code>pointerup</code></td><td><code>mouseup</code></td></tr><tr><td><code>pointermove</code></td><td><code>mousemove</code></td></tr><tr><td><code>pointerover</code></td><td><code>mouseover</code></td></tr><tr><td><code>pointerout</code></td><td><code>mouseout</code></td></tr><tr><td><code>pointerenter</code></td><td><code>mouseenter</code></td></tr><tr><td><code>pointerleave</code></td><td><code>mouseleave</code></td></tr><tr><td><code>pointercancel</code></td><td>-</td></tr><tr><td><code>gotpointercapture</code></td><td>-</td></tr><tr><td><code>lostpointercapture</code></td><td>-</td></tr></tbody></table><p>不难发现，每一个 <code>mouse&amp;lt;event&amp;gt;</code> 都有与之相对应的 <code>pointer&amp;lt;event&amp;gt;</code>。同时还有 3 个额外的事件没有相应的 <code>mouse...</code>，我们会在稍后详细解释它们。</p><p>在代码中用 <code>pointer&amp;lt;event&amp;gt;</code> 替换 <code>mouse&amp;lt;event&amp;gt;</code></p><p>我们可以把代码中的 <code>mouse&amp;lt;event&amp;gt;</code> 都替换成 <code>pointer&amp;lt;event&amp;gt;</code>，程序仍然正常兼容鼠标设备。</p><p>替换之后，程序对触屏设备的支持会“魔法般”地提升。但是，我们可能需要在 CSS 中的某些地方添加 <code>touch-action: none</code>。我们会在下文的 <code>pointercancel</code> 一节中描述这里面的细节。</p><hr><h2 id="指针事件属性" tabindex="-1"><a class="header-anchor" href="#指针事件属性"><span>指针事件属性</span></a></h2><p>指针事件具备和鼠标事件完全相同的属性，包括 <code>clientX/Y</code> 和 <code>target</code> 等，以及一些其他属性：</p><ul><li><code>pointerId</code> —— 触发当前事件的指针唯一标识符。 浏览器生成的。使我们能够处理多指针的情况，例如带有触控笔和多点触控功能的触摸屏（下文会有相关示例）。</li><li><code>pointerType</code> —— 指针的设备类型。必须为字符串，可以是：“mouse”、“pen” 或 “touch”。 我们可以使用这个属性来针对不同类型的指针输入做出不同响应。</li><li><code>isPrimary</code> —— 当指针为首要指针（多点触控时按下的第一根手指）时为 <code>true</code>。</li></ul><p>有些指针设备会测量接触面积和点按压力（例如一根手指压在触屏上），对于这种情况可以使用以下属性：</p><ul><li><code>width</code> —— 指针（例如手指）接触设备的区域的宽度。对于不支持的设备（如鼠标），这个值总是 <code>1</code></li><li><code>height</code> —— 指针（例如手指）接触设备的区域的长度。对于不支持的设备，这个值总是 <code>1</code></li><li><code>pressure</code> —— 触摸压力，是一个介于 0 到 1 之间的浮点数。对于不支持压力检测的设备，这个值总是 <code>0.5</code>（按下时）或 <code>0</code></li><li><code>tangentialPressure</code> —— 归一化后的切向压力（tangential pressure）</li><li><code>tiltX</code>, <code>tiltY</code>, <code>twist</code> —— 针对触摸笔的几个属性，用于描述笔和屏幕表面的相对位置</li></ul><p>大多数设备都不支持这些属性，因此它们很少被使用。如果你需要使用它们，可以在 <a href="https://w3c.github.io/pointerevents/#pointerevent-interface" target="_blank" rel="noopener noreferrer">规范文档</a> 中查看更多有关它们的详细信息。</p><hr><h2 id="多点触控" tabindex="-1"><a class="header-anchor" href="#多点触控"><span>多点触控</span></a></h2><p>多点触控（用户在手机或平板上同时点击若干个位置，或执行特殊手势）是鼠标事件完全不支持的功能之一。</p><p>指针事件使我们能够通过 <code>pointerId</code> 和 <code>isPrimary</code> 属性的帮助，能够处理多点触控。</p><p>当用户用一根手指触摸触摸屏的某个位置，然后将另一根手指放在该触摸屏的其他位置时，会发生以下情况：</p><ol><li>第一个手指触摸： <ul><li><code>pointerdown</code> 事件触发，<code>isPrimary=true</code>，并且被指派了一个 <code>pointerId</code>。</li></ul></li><li>第二个和后续的更多个手指触摸（假设第一个手指仍在触摸）： <ul><li><code>pointerdown</code> 事件触发，<code>isPrimary=false</code>，并且每一个触摸都被指派了不同的 <code>pointerId</code>。</li></ul></li></ol><hr><h2 id="事件-pointercancel" tabindex="-1"><a class="header-anchor" href="#事件-pointercancel"><span>事件：pointercancel</span></a></h2><p><code>pointercancel</code> 事件将会在一个正处于活跃状态的指针交互由于某些原因被中断时触发。也就是在这个事件之后，该指针就不会继续触发更多事件了。</p><p>导致指针中断的可能原因如下：</p><ul><li>指针设备硬件在物理层面上被禁用。</li><li>设备方向旋转（例如给平板转了个方向）。</li><li>浏览器打算自行处理这一交互，比如将其看作是一个专门的鼠标手势或缩放操作等。</li></ul><p>我们会用一个实际例子来阐释 <code>pointercancel</code> 的影响。</p><p>例如，我们想要实现一个像 <a href="https://zh.javascript.info/mouse-drag-and-drop" target="_blank" rel="noopener noreferrer">鼠标拖放事件</a> 中开头提到的那样的一个对球的拖放操作。</p><p>用户的操作流和对应的事件如下：</p><ol><li>用户按住了一张图片，开始拖拽 <ul><li><code>pointerdown</code> 事件触发</li></ul></li><li>用户开始移动指针（从而拖动图片） <ul><li><code>pointermove</code> 事件触发，可能触发多次</li></ul></li><li>然后意料之外的情况发生了！浏览器有自己原生的图片拖放操作，接管了之前的拖放过程，于是触发了 <code>pointercancel</code> 事件。 <ul><li>现在拖放图片的操作由浏览器自行实现。用户甚至可能会把图片拖出浏览器，放进他们的邮件程序或文件管理器。</li><li>我们不会再得到 <code>pointermove</code> 事件了。</li></ul></li></ol><p>这里的问题就在于浏览器”劫持“了这一个互动操作：在“拖放”过程开始时触发了 <code>pointercancel</code> 事件，并且不再有 <code>pointermove</code> 事件会被生成。</p><p><strong>阻止浏览器的默认行为来防止 <code>pointercancel</code> 触发。</strong></p><p>我们需要做两件事：</p><ol><li>阻止原生的拖放操作发生： <ul><li>正如我们在 <a href="https://zh.javascript.info/mouse-drag-and-drop" target="_blank" rel="noopener noreferrer">鼠标拖放事件</a> 中描述的那样，我们可以通过设置 <code>ball.ondragstart = () =&amp;gt; false</code> 来实现这一需求。</li><li>这种方式也适用于鼠标事件。</li></ul></li><li>对于触屏设备，还有其他和触摸相关的浏览器行为（除了拖放）。为了避免它们所引发的问题： <ul><li>我们可以通过在 CSS 中设置 <code>#ball { touch-action: none }</code> 来阻止它们。</li><li>之后我们的代码便可以在触屏设备中正常工作了。</li></ul></li></ol><hr><h2 id="指针捕获" tabindex="-1"><a class="header-anchor" href="#指针捕获"><span>指针捕获</span></a></h2><p>指针捕获（Pointer capturing）是针对指针事件的一个特性。</p><p>这个想法很简单，但是乍一看可能感觉很奇怪，因为在其他任何事件类型中都没有这种东西。</p><p>主要的方法是：</p><ul><li><code>elem.setPointerCapture(pointerId)</code> —— 将给定的 <code>pointerId</code> 绑定到 <code>elem</code>。在调用之后，所有具有相同 <code>pointerId</code> 的指针事件都将 <code>elem</code> 作为目标（就像事件发生在 <code>elem</code> 上一样），无论这些 <code>elem</code> 在文档中的实际位置是什么。</li></ul><p>换句话说，<code>elem.setPointerCapture(pointerId)</code> 将所有具有给定 <code>pointerId</code> 的后续事件重新定位到 <code>elem</code>。</p><p>绑定会在以下情况下被移除：</p><ul><li>当 <code>pointerup</code> 或 <code>pointercancel</code> 事件出现时，绑定会被自动地移除。</li><li>当 <code>elem</code> 被从文档中移除后，绑定会被自动地移除。</li><li>当 <code>elem.releasePointerCapture(pointerId)</code> 被调用，绑定会被移除。</li></ul><p>那么，它有什么用？我们一起来看一个实际的例子吧。</p><p><strong>指针捕获可以被用于简化拖放类的交互。</strong></p><p>让我们来回忆一下在 <a href="https://zh.javascript.info/mouse-drag-and-drop" target="_blank" rel="noopener noreferrer">鼠标拖放事件</a> 中提到的，如何实现一个自定义滑动条。</p><p>我们可以创建一个带有条形图的、并且内部有一个“滑块”（<code>thumb</code>）的滑动条元素（<code>slider</code>）：</p><div class="language-markup line-numbers-mode" data-highlighter="prismjs" data-ext="markup"><pre><code class="language-markup"><span class="line"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span> <span class="token attr-name">class</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>slider<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span></span>
<span class="line">  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span> <span class="token attr-name">class</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>thumb<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">&gt;</span></span></span>
<span class="line"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">&gt;</span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>添加样式后的效果如下：</p><p><a href="https://plnkr.co/edit/N8tLC5F0Fc4Kmi7m?p=preview" title="在沙箱中打开" target="_blank" rel="noopener noreferrer"></a></p><p>用指针事件替换鼠标事件后的实现逻辑：</p><ol><li>用户按下滑动条的滑块 <code>thumb</code> —— <code>pointerdown</code> 事件被触发。</li><li>然后用户移动指针 —— <code>pointermove</code> 事件被触发，我们让移动事件只作用在 <code>thumb</code> 上。 <ul><li>……在指针的移动过程中，指针可能会离开滑动条的 <code>thumb</code> 元素，移动到 <code>thumb</code> 之上或之下的位置。而 <code>thumb</code> 应该严格在水平方向上移动，并与指针保持对齐。</li></ul></li></ol><p>在基于鼠标事件实现的方案中，要跟踪指针的所有移动，包括指针移动到 <code>thumb</code> 之上或之下的位置时，我们必须在整个文档 <code>document</code> 上分配 <code>mousemove</code> 事件处理程序。</p><p>不过，这并不是一个没有副作用的解决方案。其中的一个问题就是，指针在文档周围的移动可能会引起副作用，在其他元素上触发事件处理程序（例如 <code>mouseover</code>）并调用其他元素上与滑动条不相关的功能，这不是我们预期的效果。</p><p>这就是 <code>setPointerCapture</code> 适用的场景。</p><ul><li>我们可以在 <code>pointerdown</code> 事件的处理程序中调用 <code>thumb.setPointerCapture(event.pointerId)</code>，</li><li>这样接下来在 <code>pointerup/cancel</code> 之前发生的所有指针事件都会被重定向到 <code>thumb</code> 上。</li><li>当 <code>pointerup</code> 发生时（拖动完成），绑定会被自动移除，我们不需要关心它。</li></ul><p>因此，即使用户在整个文档上移动指针，事件处理程序也将仅在 <code>thumb</code> 上被调用。尽管如此，事件对象的坐标属性，例如 <code>clientX/clientY</code> 仍将是正确的 —— 捕获仅影响 <code>target/currentTarget</code>。</p><p>主要代码如下：</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line">thumb<span class="token punctuation">.</span><span class="token function-variable function">onpointerdown</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token parameter">event</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token comment">// 把所有指针事件（pointerup 之前发生的）重定向到 thumb</span></span>
<span class="line">  thumb<span class="token punctuation">.</span><span class="token function">setPointerCapture</span><span class="token punctuation">(</span>event<span class="token punctuation">.</span>pointerId<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">  <span class="token comment">// 开始跟踪指针的移动</span></span>
<span class="line">  thumb<span class="token punctuation">.</span><span class="token function-variable function">onpointermove</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token parameter">event</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// 移动滑动条：在 thumb 上监听即可，因为所有指针事件都被重定向到了 thumb</span></span>
<span class="line">    <span class="token keyword">let</span> newLeft <span class="token operator">=</span> event<span class="token punctuation">.</span>clientX <span class="token operator">-</span> slider<span class="token punctuation">.</span><span class="token function">getBoundingClientRect</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>left<span class="token punctuation">;</span></span>
<span class="line">    thumb<span class="token punctuation">.</span>style<span class="token punctuation">.</span>left <span class="token operator">=</span> newLeft <span class="token operator">+</span> <span class="token string">&#39;px&#39;</span><span class="token punctuation">;</span></span>
<span class="line">  <span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">  <span class="token comment">// 当结束(pointerup)时取消对指针移动的跟踪</span></span>
<span class="line">  thumb<span class="token punctuation">.</span><span class="token function-variable function">onpointerup</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token parameter">event</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    thumb<span class="token punctuation">.</span>onpointermove <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span></span>
<span class="line">    thumb<span class="token punctuation">.</span>onpointerup <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token comment">// ...这里还可以处理“拖动结束”相关的逻辑</span></span>
<span class="line">  <span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 注意：无需调用 thumb.releasePointerCapture，</span></span>
<span class="line"><span class="token comment">// 它会在 pointerup 时被自动调用</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>完整示例：</p><p><a href="https://plnkr.co/edit/2Yc1R1oCgXrepdId?p=preview" title="在沙箱中打开" target="_blank" rel="noopener noreferrer"></a></p><p>在这个 demo 中还有一个元素，当它的 <code>onmouseover</code> 处理程序被触发时会显示当前的时间。</p><p>请注意：当你拖动滑块的时候，鼠标可能会悬停在这个元素上，它的 <code>onmouseover</code> 处理程序<strong>不会</strong>被触发。</p><p>借助于 <code>setPointerCapture</code>，现在拖动滑块不会再产生副作用了。</p><p>言而总之，指针捕获为我们带来了两个好处：</p><ol><li>代码变得更加简洁，我们不再需要在整个 <code>document</code> 上添加/移除处理程序。绑定会被自动释放。</li><li>如果文档中有其他指针事件处理程序，则在用户拖动滑动条时，它们不会因指针的移动被意外地触发。</li></ol><h3 id="指针捕获事件" tabindex="-1"><a class="header-anchor" href="#指针捕获事件"><span><a href="https://zh.javascript.info/pointer-events#zhi-zhen-bu-huo-shi-jian" target="_blank" rel="noopener noreferrer">指针捕获事件</a></span></a></h3><p>完整起见，这里还需要提及一个知识点。</p><p>还有两个与指针捕获相关的事件：</p><ul><li><code>gotpointercapture</code> 会在一个元素使用 <code>setPointerCapture</code> 来启用捕获后触发。</li><li><code>lostpointercapture</code> 会在捕获被释放后触发：其触发可能是由于 <code>releasePointerCapture</code> 的显式调用，或是 <code>pointerup</code>/<code>pointercancel</code> 事件触发后的自动调用。</li></ul>`,79)])])}const i=n(t,[["render",p]]),d=JSON.parse('{"path":"/notes/obsidian/%E5%89%8D%E7%AB%AF/%E6%B5%8F%E8%A7%88%E5%99%A8_%E7%BD%91%E7%BB%9C/%E6%B5%8F%E8%A7%88%E5%99%A8%E7%8E%AF%E5%A2%83/Web%20API/%E4%BA%8B%E4%BB%B6/UI%E4%BA%8B%E4%BB%B6/%E6%8C%87%E9%92%88%E4%BA%8B%E4%BB%B6.html","title":"指针事件","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"notes/obsidian/前端/浏览器&网络/浏览器环境/Web API/事件/UI事件/指针事件.md"}');export{i as comp,d as data};
