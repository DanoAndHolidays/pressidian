import{_ as e,c as s,b as a,o}from"./app-DfSzGDtR.js";const t={};function c(l,n){return o(),s("div",null,[...n[0]||(n[0]=[a(`<h1 id="window大小与滚动" tabindex="-1"><a class="header-anchor" href="#window大小与滚动"><span>Window大小与滚动</span></a></h1><p>&gt; Last Format Time：6/21/2026 17:22:15</p><hr><h2 id="窗口的-width-height" tabindex="-1"><a class="header-anchor" href="#窗口的-width-height"><span>窗口的 width/height</span></a></h2><p>为了获取窗口（window）的宽度和高度，我们可以使用 <code>document.documentElement</code> 的 <code>clientWidth/clientHeight</code></p><p><code>window.innerWidth/innerHeight</code> 包括了滚动条。</p><p>如果这里有一个滚动条，它占用了一些空间，那么这两行代码会显示不同的值：</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token function">alert</span><span class="token punctuation">(</span> window<span class="token punctuation">.</span>innerWidth <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 整个窗口的宽度</span></span>
<span class="line"><span class="token function">alert</span><span class="token punctuation">(</span> document<span class="token punctuation">.</span>documentElement<span class="token punctuation">.</span>clientWidth <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 减去滚动条宽度后的窗口宽度</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>在大多数情况下，我们需要 <strong>可用</strong> 的窗口宽度以绘制或放置某些东西。也就是说，在滚动条内（如果有）。所以，我们应该使用 <code>documentElement.clientHeight/clientWidth</code>。</p><h3 id="doctype-很重要" tabindex="-1"><a class="header-anchor" href="#doctype-很重要"><span><code>DOCTYPE</code> 很重要</span></a></h3><p>请注意：当 HTML 中没有 <code>&amp;lt;!DOCTYPE HTML&amp;gt;</code> 时，顶层级（top-level）几何属性的工作方式可能就会有所不同。可能会出现一些稀奇古怪的情况。在现代 HTML 中，我们始终都应该写 <code>DOCTYPE</code>。</p><hr><h2 id="文档的-width-height" tabindex="-1"><a class="header-anchor" href="#文档的-width-height"><span>文档的 width/height</span></a></h2><p>从理论上讲，由于根文档元素是 <code>document.documentElement</code>，并且它包围了所有内容，因此我们可以通过使用 <code>documentElement.scrollWidth/scrollHeight</code> 来测量文档的完整大小。</p><p>但是在该元素上，对于整个文档，这些属性均无法正常工作。在 Chrome/Safari/Opera 中，如果没有滚动条，<code>documentElement.scrollHeight</code> 甚至可能小于 <code>documentElement.clientHeight</code>！很奇怪，对吧？</p><p>为了可靠地获得完整的文档高度，我们应该采用以下这些属性的最大值：</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token keyword">let</span> scrollHeight <span class="token operator">=</span> Math<span class="token punctuation">.</span><span class="token function">max</span><span class="token punctuation">(</span></span>
<span class="line">  document<span class="token punctuation">.</span>body<span class="token punctuation">.</span>scrollHeight<span class="token punctuation">,</span> document<span class="token punctuation">.</span>documentElement<span class="token punctuation">.</span>scrollHeight<span class="token punctuation">,</span></span>
<span class="line">  document<span class="token punctuation">.</span>body<span class="token punctuation">.</span>offsetHeight<span class="token punctuation">,</span> document<span class="token punctuation">.</span>documentElement<span class="token punctuation">.</span>offsetHeight<span class="token punctuation">,</span></span>
<span class="line">  document<span class="token punctuation">.</span>body<span class="token punctuation">.</span>clientHeight<span class="token punctuation">,</span> document<span class="token punctuation">.</span>documentElement<span class="token punctuation">.</span>clientHeight</span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token function">alert</span><span class="token punctuation">(</span><span class="token string">&#39;Full document height, with scrolled out part: &#39;</span> <span class="token operator">+</span> scrollHeight<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>为什么这样？最好不要问。这些不一致来源于远古时代，而不是“聪明”的逻辑。</p><hr><h2 id="获得当前滚动" tabindex="-1"><a class="header-anchor" href="#获得当前滚动"><span>获得当前滚动</span></a></h2><p>DOM 元素的当前滚动状态在其 <code>scrollLeft/scrollTop</code> 属性中。</p><p>对于文档滚动，在大多数浏览器中，我们可以使用 <code>document.documentElement.scrollLeft/scrollTop</code>，但在较旧的基于 WebKit 的浏览器中则不行，例如在 Safari（bug <a href="https://bugs.webkit.org/show_bug.cgi?id=5991" target="_blank" rel="noopener noreferrer">5991</a>）中，我们应该使用 <code>document.body</code> 而不是 <code>document.documentElement</code>。</p><p>幸运的是，我们根本不必记住这些特性，因为我们可以从 <code>window.pageXOffset/pageYOffset</code> 中获取页面当前滚动信息：</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token function">alert</span><span class="token punctuation">(</span><span class="token string">&#39;当前已从顶部滚动：&#39;</span> <span class="token operator">+</span> window<span class="token punctuation">.</span>pageYOffset<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token function">alert</span><span class="token punctuation">(</span><span class="token string">&#39;当前已从左侧滚动：&#39;</span> <span class="token operator">+</span> window<span class="token punctuation">.</span>pageXOffset<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>这些属性是只读的。我们也可以从 <code>window</code> 的 <code>scrollX</code> 和 <code>scrollY</code> 属性中获取滚动信息</p><p>由于历史原因，存在了这两种属性，但它们是一样的：</p><ul><li><code>window.pageXOffset</code> 是 <code>window.scrollX</code> 的别名。</li><li><code>window.pageYOffset</code> 是 <code>window.scrollY</code> 的别名。</li></ul><hr><h2 id="滚动-scrollto-scrollby-scrollintoview" tabindex="-1"><a class="header-anchor" href="#滚动-scrollto-scrollby-scrollintoview"><span>滚动：scrollTo，scrollBy，scrollIntoView</span></a></h2><p>必须在 DOM 完全构建好之后才能通过 JavaScript 滚动页面。如果我们尝试通过 <code>&amp;lt;head&amp;gt;</code> 中的脚本滚动页面，它将无法正常工作。</p><p>可以通过更改 <code>scrollTop/scrollLeft</code> 来滚动常规元素。</p><p>我们可以使用 <code>document.documentElement.scrollTop/scrollLeft</code> 对页面进行相同的操作（Safari 除外，而应该使用 <code>document.body.scrollTop/Left</code> 代替）。</p><p>或者，有一个更简单的通用解决方案：使用特殊方法 <a href="https://developer.mozilla.org/zh/docs/Web/API/Window/scrollBy" target="_blank" rel="noopener noreferrer">window.scrollBy(x,y)</a> 和 <a href="https://developer.mozilla.org/zh/docs/Web/API/Window/scrollTo" target="_blank" rel="noopener noreferrer">window.scrollTo(pageX,pageY)</a>。</p><ul><li>方法 <code>scrollBy(x,y)</code> 将页面滚动至 <strong>相对于当前位置的 <code>(x, y)</code> 位置</strong>。例如，<code>scrollBy(0,10)</code> 会将页面向下滚动 <code>10px</code>。</li><li>方法 <code>scrollTo(pageX,pageY)</code> 将页面滚动至 <strong>绝对坐标</strong>，使得可见部分的左上角具有相对于文档左上角的坐标 <code>(pageX, pageY)</code>。就像设置了 <code>scrollLeft/scrollTop</code> 一样。</li></ul><p>这些方法适用于所有浏览器。</p><hr><h2 id="scrollintoview" tabindex="-1"><a class="header-anchor" href="#scrollintoview"><span>scrollIntoView</span></a></h2><p>为了完整起见，让我们再介绍一种方法：<a href="https://developer.mozilla.org/zh/docs/Web/API/Element/scrollIntoView" target="_blank" rel="noopener noreferrer">elem.scrollIntoView(top)</a>。</p><p>对 <code>elem.scrollIntoView(top)</code> 的调用将滚动页面以使 <code>elem</code> 可见。它有一个参数：</p><ul><li>如果 <code>top=true</code>（默认值），页面滚动，使 <code>elem</code> 出现在窗口顶部。元素的上边缘将与窗口顶部对齐。</li><li>如果 <code>top=false</code>，页面滚动，使 <code>elem</code> 出现在窗口底部。元素的底部边缘将与窗口底部对齐。</li></ul><hr><h2 id="禁止滚动" tabindex="-1"><a class="header-anchor" href="#禁止滚动"><span>禁止滚动</span></a></h2><p>要使文档不可滚动，只需要设置 <code>document.body.style.overflow = &quot;hidden&quot;</code>。该页面将“冻结”在其当前滚动位置上。</p><p><code>document.body.style.overflow = ‘hidden’</code><code>document.body.style.overflow = ‘’</code></p><p>第一个按钮用于冻结滚动，第二个按钮则用于恢复滚动。</p><p>我们还可以使用相同的技术来冻结其他元素的滚动，而不仅仅是 <code>document.body</code>。</p><p>这个方法的缺点是会使滚动条消失。如果滚动条占用了一些空间，它原本占用的空间就会空出来，那么内容就会“跳”进去以填充它。</p><p>这看起来有点奇怪，但是我们可以对比冻结前后的 <code>clientWidth</code>。如果它增加了（滚动条消失后），那么我们可以在 <code>document.body</code> 中滚动条原来的位置处通过添加 <code>padding</code>，来替代滚动条，这样这个问题就解决了。保持了滚动条冻结前后文档内容宽度相同。</p><hr><h2 id="总结" tabindex="-1"><a class="header-anchor" href="#总结"><span>总结</span></a></h2><p>几何：</p><ul><li>文档可见部分的 width/height（内容区域的 width/height）：<code>document.documentElement.clientWidth/clientHeight</code></li><li>整个文档的 width/height，其中包括滚动出去的部分：<div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token keyword">let</span> scrollHeight <span class="token operator">=</span> Math<span class="token punctuation">.</span><span class="token function">max</span><span class="token punctuation">(</span></span>
<span class="line">  document<span class="token punctuation">.</span>body<span class="token punctuation">.</span>scrollHeight<span class="token punctuation">,</span> document<span class="token punctuation">.</span>documentElement<span class="token punctuation">.</span>scrollHeight<span class="token punctuation">,</span></span>
<span class="line">  document<span class="token punctuation">.</span>body<span class="token punctuation">.</span>offsetHeight<span class="token punctuation">,</span> document<span class="token punctuation">.</span>documentElement<span class="token punctuation">.</span>offsetHeight<span class="token punctuation">,</span></span>
<span class="line">  document<span class="token punctuation">.</span>body<span class="token punctuation">.</span>clientHeight<span class="token punctuation">,</span> document<span class="token punctuation">.</span>documentElement<span class="token punctuation">.</span>clientHeight</span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul><p>滚动：</p><ul><li>读取当前的滚动：<code>window.pageYOffset/pageXOffset</code>。</li><li>更改当前的滚动： <ul><li><code>window.scrollTo(pageX,pageY)</code> —— 绝对坐标，</li><li><code>window.scrollBy(x,y)</code> —— 相对当前位置进行滚动，</li><li><code>elem.scrollIntoView(top)</code> —— 滚动以使 <code>elem</code> 可见（<code>elem</code> 与窗口的顶部/底部对齐）。</li></ul></li></ul>`,54)])])}const i=e(t,[["render",c]]),d=JSON.parse('{"path":"/notes/obsidian/%E5%89%8D%E7%AB%AF/%E6%B5%8F%E8%A7%88%E5%99%A8_%E7%BD%91%E7%BB%9C/%E6%B5%8F%E8%A7%88%E5%99%A8%E7%8E%AF%E5%A2%83/Web%20API/DOM/Window%E5%A4%A7%E5%B0%8F%E4%B8%8E%E6%BB%9A%E5%8A%A8.html","title":"Window大小与滚动","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"notes/obsidian/前端/浏览器&网络/浏览器环境/Web API/DOM/Window大小与滚动.md"}');export{i as comp,d as data};
