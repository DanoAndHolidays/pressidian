import{_ as s,c as a,b as e,o as t}from"./app-DR5jxWn5.js";const p={};function l(o,n){return t(),a("div",null,[...n[0]||(n[0]=[e(`<h1 id="fiber" tabindex="-1"><a class="header-anchor" href="#fiber"><span>Fiber</span></a></h1><p><a href="https://www.bilibili.com/video/BV1mCqEBxEBv/?spm_id_from=333.337.search-card.all.click&amp;vd_source=47c9acd507be61251cd2bb730416395c" target="_blank" rel="noopener noreferrer">https://www.bilibili.com/video/BV1mCqEBxEBv/?spm_id_from=333.337.search-card.all.click&amp;vd_source=47c9acd507be61251cd2bb730416395c</a></p><ul><li>实现了增量渲染，避免了主线程的阻塞</li><li>实现了更新的优先级： <ul><li>最高：用户打字，点击</li><li>中级：动画更新</li><li>低级：网络请求</li></ul></li><li>作为一个鸡架，为后续的特性铺平了道路，提供了更多的可能性</li></ul><h2 id="react-fiber-的核心原理" tabindex="-1"><a class="header-anchor" href="#react-fiber-的核心原理"><span>React Fiber 的核心原理</span></a></h2><p>React 16 引入的 <strong>Fiber 架构</strong>，核心目标是实现<strong>可中断的异步渲染</strong>，解决旧版栈调和（Stack Reconciler）在复杂页面中卡顿掉帧的问题。</p><p>这里实际上是用到了浏览器留的一个后门</p><h3 id="一、旧架构的问题-同步不可中断" tabindex="-1"><a class="header-anchor" href="#一、旧架构的问题-同步不可中断"><span>一、旧架构的问题：同步不可中断</span></a></h3><p>旧版 React 使用递归调用栈进行虚拟 DOM 比对（Reconciliation）。一旦开始渲染，就会<strong>同步、递归</strong>地处理整个组件树，直到完整计算完所有更新才能让出浏览器主线程。</p><p>如果组件树很深、更新频繁，这个“大任务”会长时间占用主线程，导致用户输入、动画等无法及时响应，产生明显的卡顿。</p><h3 id="二、fiber-的核心思想-任务拆分-可中断-优先级" tabindex="-1"><a class="header-anchor" href="#二、fiber-的核心思想-任务拆分-可中断-优先级"><span>二、Fiber 的核心思想：任务拆分 + 可中断 + 优先级</span></a></h3><p>Fiber 将渲染工作拆分成一个个<strong>小的任务单元</strong>，每个单元完成后可以主动让出主线程，让浏览器先处理更高优先级的任务（如用户点击、动画），之后恢复未完成的工作。</p><p>这需要解决三个问题：</p><ol><li>如何把递归遍历拆成可暂停的单元？</li><li>如何知道什么时候该暂停/恢复？</li><li>如何协调不同任务的优先级？</li></ol><h3 id="三、fiber-节点的数据结构" tabindex="-1"><a class="header-anchor" href="#三、fiber-节点的数据结构"><span>三、Fiber 节点的数据结构</span></a></h3><p>每个 React 元素（组件、DOM 节点）都会对应一个 <strong>Fiber 节点</strong>。它是一个普通的 JavaScript 对象，记录了组件的类型、状态、副作用以及<strong>链表结构</strong>的连接关系。</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token comment">// 简化版 Fiber 节点</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token comment">// 节点类型（HostComponent, ClassComponent, FunctionComponent...）</span></span>
<span class="line">  <span class="token literal-property property">tag</span><span class="token operator">:</span> WorkTag<span class="token punctuation">,</span></span>
<span class="line">  <span class="token comment">// 关联的真实 DOM 或组件实例</span></span>
<span class="line">  <span class="token literal-property property">stateNode</span><span class="token operator">:</span> <span class="token operator">...</span><span class="token punctuation">,</span></span>
<span class="line">  </span>
<span class="line">  <span class="token comment">// 组件自身的状态（props, state, hooks）</span></span>
<span class="line">  <span class="token literal-property property">pendingProps</span><span class="token operator">:</span> nextProps<span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">memoizedProps</span><span class="token operator">:</span> prevProps<span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">memoizedState</span><span class="token operator">:</span> prevState<span class="token punctuation">,</span></span>
<span class="line">  </span>
<span class="line">  <span class="token comment">// 链表连接</span></span>
<span class="line">  <span class="token keyword">return</span><span class="token operator">:</span> parentFiber<span class="token punctuation">,</span>      <span class="token comment">// 父节点</span></span>
<span class="line">  <span class="token literal-property property">child</span><span class="token operator">:</span> firstChildFiber<span class="token punctuation">,</span>   <span class="token comment">// 第一个子节点</span></span>
<span class="line">  <span class="token literal-property property">sibling</span><span class="token operator">:</span> nextSiblingFiber<span class="token punctuation">,</span><span class="token comment">// 兄弟节点</span></span>
<span class="line">  </span>
<span class="line">  <span class="token comment">// 工作进度</span></span>
<span class="line">  <span class="token literal-property property">alternate</span><span class="token operator">:</span> currentFiber<span class="token punctuation">,</span>  <span class="token comment">// 双缓冲中指向另一棵树的对应节点</span></span>
<span class="line">  </span>
<span class="line">  <span class="token comment">// 副作用列表</span></span>
<span class="line">  <span class="token literal-property property">effectTag</span><span class="token operator">:</span> <span class="token string">&#39;Placement&#39;</span> <span class="token operator">|</span> <span class="token string">&#39;Update&#39;</span> <span class="token operator">|</span> <span class="token string">&#39;Deletion&#39;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token literal-property property">nextEffect</span><span class="token operator">:</span> nextEffectNode<span class="token punctuation">,</span></span>
<span class="line">  </span>
<span class="line">  <span class="token comment">// 调度优先级</span></span>
<span class="line">  <span class="token literal-property property">lanes</span><span class="token operator">:</span> Lanes<span class="token punctuation">,</span>  <span class="token comment">// React 17+ 使用 lane 模型替代 expirationTime</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>每个 Fiber 节点通过 <code>child</code>、<code>sibling</code>、<code>return</code> 形成一棵 <strong>Fiber 树</strong>。这种链表结构使得遍历可以随时暂停、恢复，而不必依赖函数调用栈。</p><h3 id="四、双缓冲技术-current-workinprogress" tabindex="-1"><a class="header-anchor" href="#四、双缓冲技术-current-workinprogress"><span>四、双缓冲技术（Current &amp; WorkInProgress）</span></a></h3><p>React 内存中同时维护两棵 Fiber 树：</p><ul><li><strong>current 树</strong>：当前屏幕上已经渲染的内容对应的 Fiber 树。</li><li><strong>workInProgress 树</strong>：正在内存中构建的新树，代表未来将要渲染的内容。</li></ul><p>每次更新（setState、props 变化等）都会基于 current 树克隆出 workInProgress 树，然后在 workInProgress 树上进行协调计算。计算完成后，用 workInProgress 树直接替换 current 树（称为“提交”阶段）。这个过程类似图形学中的双缓冲，避免了 UI 出现不完整渲染。</p><p>Fiber 节点通过 <code>alternate</code> 属性指向自己在另一棵树中的对应节点，以便复用状态。</p><h3 id="五、工作循环-中断与恢复" tabindex="-1"><a class="header-anchor" href="#五、工作循环-中断与恢复"><span>五、工作循环：中断与恢复</span></a></h3><p>React 的调度与工作循环分布在两个主要阶段：<strong>渲染阶段（Reconciliation Phase）</strong> 和 <strong>提交阶段（Commit Phase）</strong>。</p><h4 id="_1-渲染阶段-可中断" tabindex="-1"><a class="header-anchor" href="#_1-渲染阶段-可中断"><span>1. 渲染阶段（可中断）</span></a></h4><p>这个阶段负责找出哪些节点发生了改变（增、删、改），并为它们打上 <code>effectTag</code>。整个过程是<strong>异步可中断</strong>的。</p><p>核心代码逻辑（简化）：</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token keyword">let</span> nextUnitOfWork <span class="token operator">=</span> rootFiber<span class="token punctuation">;</span>   <span class="token comment">// 下一个待处理的 Fiber 节点</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">function</span> <span class="token function">workLoop</span><span class="token punctuation">(</span><span class="token parameter">deadline</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token comment">// 是否应该暂停（让出主线程）</span></span>
<span class="line">  <span class="token keyword">let</span> shouldYield <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span></span>
<span class="line">  <span class="token keyword">while</span> <span class="token punctuation">(</span>nextUnitOfWork <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span>shouldYield<span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// 执行当前工作单元，并返回下一个工作单元</span></span>
<span class="line">    nextUnitOfWork <span class="token operator">=</span> <span class="token function">performUnitOfWork</span><span class="token punctuation">(</span>nextUnitOfWork<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token comment">// 检查剩余时间是否不足</span></span>
<span class="line">    shouldYield <span class="token operator">=</span> deadline<span class="token punctuation">.</span><span class="token function">timeRemaining</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">&lt;</span> <span class="token number">1</span><span class="token punctuation">;</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line">  <span class="token comment">// 如果没有剩余工作单元，说明渲染阶段完成，可以进入提交阶段</span></span>
<span class="line">  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>nextUnitOfWork<span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token function">commitRoot</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line">  <span class="token comment">// 否则继续请求下一帧执行</span></span>
<span class="line">  <span class="token function">requestIdleCallback</span><span class="token punctuation">(</span>workLoop<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 开始调度</span></span>
<span class="line"><span class="token function">requestIdleCallback</span><span class="token punctuation">(</span>workLoop<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>performUnitOfWork</code> 会处理当前 Fiber 节点（调用函数组件、协调子节点等），然后根据 <code>child</code> / <code>sibling</code> 返回下一个需要处理的节点。</li><li><strong>时间切片</strong>：React 使用 <code>requestIdleCallback</code>（polyfill 或用 <code>MessageChannel</code> 实现）来获取浏览器空闲时段。每个工作单元执行后检查剩余时间，不足则主动交还控制权。</li></ul><h4 id="_2-提交阶段-同步不可中断" tabindex="-1"><a class="header-anchor" href="#_2-提交阶段-同步不可中断"><span>2. 提交阶段（同步不可中断）</span></a></h4><p>当 <code>nextUnitOfWork === null</code> 表示所有 Fiber 节点处理完毕，此时进入提交阶段。提交阶段会遍历“副作用链表”（通过 <code>nextEffect</code> 连接所有需要更新的节点），一次性将 DOM 变更应用到真实 DOM 上。这个过程是<strong>同步的</strong>，保证 UI 最终状态一致。</p><h3 id="六、优先级与调度" tabindex="-1"><a class="header-anchor" href="#六、优先级与调度"><span>六、优先级与调度</span></a></h3><p>为了让高优先级任务（如动画、输入）能打断低优先级任务（如数据请求后的渲染），React 设计了<strong>优先级模型</strong>（React 17+ 使用 Lane 模型）。</p><ul><li>每个更新任务被赋予一个 <strong>lane</strong>（二进制位，如 <code>SyncLane</code>、<code>InputContinuousLane</code>、<code>DefaultLane</code> 等）。</li><li>调度器会维护一个任务队列，每次循环时取出优先级最高的任务执行。</li><li>若高优先级任务出现，当前正在进行的低优先级工作会被中断，并且其 workInProgress 树会被丢弃；然后重新基于 current 树为高优先级任务克隆新的 workInProgress 树。</li></ul><p>这个优先级机制让 React 可以“先响应用户，再慢慢算数据”。</p><h3 id="七、完整的渲染流程示例" tabindex="-1"><a class="header-anchor" href="#七、完整的渲染流程示例"><span>七、完整的渲染流程示例</span></a></h3><ol><li>调用 <code>setState</code> 产生一个更新，进入 <strong>调度器</strong>（Scheduler）。</li><li>调度器根据优先级为该更新分配一个 lane，并放入任务队列。</li><li>浏览器空闲时，调度器从队列中取出最高优先级任务，开始<strong>渲染阶段</strong>。</li><li>构建 workInProgress 树： <ul><li>从 rootFiber 开始，调用 <code>performUnitOfWork</code> 深度优先遍历。</li><li>遇到类组件调用其 render 方法；函数组件调用函数自身。</li><li>对比 current 树上的旧 Fiber 与新的 React 元素，决定是复用、更新还是删除节点。</li><li>每处理完一个节点，返回下一个待处理的节点（child 或 sibling）。</li><li>每次检查剩余时间，不够则暂停循环，保存 <code>nextUnitOfWork</code> 等待下一帧继续。</li></ul></li><li>整个 workInProgress 树构建完成 → 所有变化以 <code>effectTag</code> 形式记录在 Fiber 的副作用链表中。</li><li><strong>提交阶段</strong>： <ul><li>遍历副作用链表，对每个 Fiber 执行 DOM 操作（如 <code>appendChild</code>、<code>removeChild</code>、<code>setAttribute</code>）。</li><li>触发生命周期（如 <code>componentDidMount</code>、<code>componentDidUpdate</code>）。</li><li>将 workInProgress 树赋值给 <code>current</code> 树，以备下次更新复用。</li></ul></li></ol><h3 id="八、与旧架构的对比" tabindex="-1"><a class="header-anchor" href="#八、与旧架构的对比"><span>八、与旧架构的对比</span></a></h3><table><thead><tr><th>特性</th><th>旧版 Stack Reconciler</th><th>Fiber Reconciler</th></tr></thead><tbody><tr><td>遍历方式</td><td>递归调用栈（不可中断）</td><td>链表遍历（可中断、恢复）</td></tr><tr><td>任务粒度</td><td>整个组件树作为一个任务</td><td>每个 Fiber 节点是独立工作单元</td></tr><tr><td>优先级</td><td>无，同步执行</td><td>支持任务优先级和抢占</td></tr><tr><td>渲染输出</td><td>全部同步更新 DOM</td><td>分两阶段：可中断的渲染 + 同步提交</td></tr><tr><td>时间切片</td><td>不支持</td><td>支持，可避免长时间阻塞主线程</td></tr></tbody></table><h3 id="九、总结" tabindex="-1"><a class="header-anchor" href="#九、总结"><span>九、总结</span></a></h3><p>React Fiber 本质上是一种<strong>重新实现协调算法</strong>的方式，通过<strong>链表结构</strong> + <strong>时间切片</strong> + <strong>双缓冲</strong> + <strong>任务优先级</strong>，把原来不可中断的递归更新打散成一个个可中断的小任务。这使得 React 应用在复杂交互和渲染之间能保持流畅，也为后续的 Concurrent Mode（并发模式）和 Suspense 等特性奠定了基础。</p>`,41)])])}const c=s(p,[["render",l]]),r=JSON.parse('{"path":"/notes/obsidian/%E5%89%8D%E7%AB%AF/%E5%BA%93_%E6%A1%86%E6%9E%B6/%E5%BA%93/React/Fiber.html","title":"Fiber","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"notes/obsidian/前端/库&框架/库/React/Fiber.md"}');export{c as comp,r as data};
