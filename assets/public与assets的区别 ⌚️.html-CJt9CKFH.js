import{_ as e,c as o,b as s,o as l}from"./app-CB1M8-_T.js";const d={};function i(c,t){return l(),o("div",null,[...t[0]||(t[0]=[s('<p>在 Vite 项目中，<code>public</code> 和 <code>src/assets</code>（或 <code>src</code> 下的其他静态资源目录）是存放静态资源的两个核心位置。它们在构建工具的处理方式、引用路径以及适用场景上有着本质的区别。</p><p>以下是它们的主要区别：</p><h3 id="_1-构建处理方式" tabindex="-1"><a class="header-anchor" href="#_1-构建处理方式"><span>1. 构建处理方式</span></a></h3><ul><li><strong><code>public</code> 目录</strong>：该目录下的资源<strong>不会经过 Vite 的构建处理</strong>。在打包时，这些文件会被原封不动地直接复制到输出目录（通常是 <code>dist</code>）的根目录下，文件名保持不变，不会被压缩、转译或添加哈希后缀。</li><li><strong><code>src</code> 目录（如 <code>src/assets</code>）</strong>：该目录下的资源会被 Vite 视为模块依赖并纳入构建管道。在打包时，Vite 会对它们进行优化处理，例如图片压缩、重命名并追加哈希值（如 <code>logo.a1b2c3.png</code>），以支持长效缓存和减小文件体积。</li></ul><h3 id="_2-引用与访问路径" tabindex="-1"><a class="header-anchor" href="#_2-引用与访问路径"><span>2. 引用与访问路径</span></a></h3><ul><li><strong><code>public</code> 目录</strong>：必须使用<strong>根绝对路径</strong>来引用。例如，<code>public/icon.png</code> 在代码中应写为 <code>/icon.png</code>。如果在 JS/TS 中使用相对路径或字符串拼接引用，可能会因为构建后路径固定而导致开发环境与生产环境不一致的问题。</li><li><strong><code>src</code> 目录</strong>：推荐使用<strong>相对路径</strong>或通过 <code>import</code> 语句显式导入。对于动态生成的路径，不能直接拼接字符串，而需要使用 <code>new URL(path, import.meta.url)</code> 或 <code>import.meta.glob</code> API 来处理，以确保 Vite 能在构建时正确解析资源。</li></ul><h3 id="_3-适用场景" tabindex="-1"><a class="header-anchor" href="#_3-适用场景"><span>3. 适用场景</span></a></h3><ul><li><strong><code>public</code> 目录</strong>：适合存放完全不需要构建处理的“原生资源”。典型场景包括： <ul><li>网站图标（<code>favicon.ico</code>）、PWA 清单（<code>site.webmanifest</code>）、搜索引擎爬虫配置（<code>robots.txt</code>）等。</li><li>必须保持原有文件名的资源（如供用户下载的 PDF 手册）。</li><li>需要通过固定绝对路径直接访问的资源，或在 HTML 模板中通过 <code>&amp;lt;script&amp;gt;</code> 标签引入的第三方 SDK 脚本。</li></ul></li><li><strong><code>src</code> 目录</strong>：适合存放项目内部业务强相关的资源。典型场景包括： <ul><li>组件中引用的图片、样式表、字体等。</li><li>需要被 Vite 压缩和优化以提升网页加载速度的媒体文件。</li><li>需要在 JavaScript/TypeScript 中作为模块导入使用的资源。</li></ul></li></ul><h3 id="总结对比" tabindex="-1"><a class="header-anchor" href="#总结对比"><span>总结对比</span></a></h3><table><thead><tr><th style="text-align:left;">特性</th><th style="text-align:left;">public 目录</th><th style="text-align:left;">src 目录 (如 src/assets)</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>处理方式</strong></td><td style="text-align:left;">不处理，原样复制到 dist 根目录</td><td style="text-align:left;">经过 Vite 处理，可能压缩、哈希化</td></tr><tr><td style="text-align:left;"><strong>引用方式</strong></td><td style="text-align:left;">必须使用根绝对路径（如 <code>/img.png</code>）</td><td style="text-align:left;">使用相对路径或 <code>import</code> 导入</td></tr><tr><td style="text-align:left;"><strong>文件名</strong></td><td style="text-align:left;">保持原样，无哈希值</td><td style="text-align:left;">默认追加哈希值，利于缓存</td></tr><tr><td style="text-align:left;"><strong>适用资源</strong></td><td style="text-align:left;">favicon、robots.txt、第三方库脚本等</td><td style="text-align:left;">业务图片、CSS、字体、需优化的媒体文件等</td></tr></tbody></table><p>简单来说，一个实用的原则是：<strong>需要 Vite 帮你处理（如哈希缓存、压缩）的资源放 <code>src/assets</code>；完全静态、需要原样复制且路径固定的资源放 <code>public</code></strong>。</p><hr><p>需要我补充一下在开发环境（Vite dev）和生产环境（Vite build）中，这两个目录的资源访问路径差异吗？实际开发中很容易踩坑。</p>',13)])])}const n=e(d,[["render",i]]),r=JSON.parse('{"path":"/notes/obsidian/%E5%89%8D%E7%AB%AF/%E5%B7%A5%E7%A8%8B%E5%8C%96/%E6%9E%84%E5%BB%BA%E6%89%93%E5%8C%85/Vite/public%E4%B8%8Eassets%E7%9A%84%E5%8C%BA%E5%88%AB%20%E2%8C%9A%EF%B8%8F.html","title":"","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"notes/obsidian/前端/工程化/构建打包/Vite/public与assets的区别 ⌚️.md"}');export{n as comp,r as data};
