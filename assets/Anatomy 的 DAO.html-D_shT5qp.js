import{_ as a,c as e,b as r,o as n}from"./app-Bmdocauq.js";const o={};function i(p,t){return n(),e("div",null,[...t[0]||(t[0]=[r('<h1 id="anatomy-的-dao" tabindex="-1"><a class="header-anchor" href="#anatomy-的-dao"><span>Anatomy 的 DAO</span></a></h1><p>❯ createWithTx 与普通的create的区别是什么呢，为什么要这样去写呢<br> ⎿ ⧉ Selected 1 lines from packages\\db\\src\\dao\\anatomies-dao.ts in Visual Studio Code</p><p>● Thinking for 6s… (ctrl+o to expand)</p><p>● 这是个非常关键的问题，理解了它才能真正会用事务。</p><hr><p>create vs createWithTx 的区别</p><p>两者做的都是同一张表的 INSERT，唯一的区别是：它们在不在同一个事务里执行。</p><p>// 普通 create：直接拿全局 db 连接插入，自己就是一个独立事务 async create(input: AnatomyInsert): Promise&lt;AnatomyRecord&gt; { const rows = await db.insert(anatomiesTable).values(input).returning(); return rows[0]!; }</p><p>// createWithTx：用调用方传进来的 tx 插入，参与到调用方的事务里 async createWithTx( tx: DatabaseTransaction, input: AnatomyInsert, ): Promise&lt;AnatomyRecord&gt; { const rows = await tx.insert(anatomiesTable).values(input).returning(); return rows[0]!; }</p><hr><p>为什么要这样写？</p><p>因为 Repository 需要把多张表的操作打包成一个原子操作。原子性的意思是：要么全部成功，要么全部回滚，不能出现“一半成功、一半失败”。</p><p>以 createDraft 为例，它必须同时做两件事：</p><ol><li>在 anatomies 表插入一条身份记录</li><li>在 anatomy_drafts 表插入一条 Draft 记录</li></ol><p>如果第一步成功了，第二步失败了，数据库里就会留下一个“没有 Draft 的 Anatomy”——这是脏数据。</p><p>所以 Repository 会这样写：</p><p>return db.transaction(async (tx) =&gt; { const anatomy = await anatomiesDao.createWithTx(tx, { id: crypto.randomUUID() }); const draft = await draftsDao.createWithTx(tx, { anatomyId: <a href="http://anatomy.id" target="_blank" rel="noopener noreferrer">anatomy.id</a>, ... }); return { anatomyId: <a href="http://anatomy.id" target="_blank" rel="noopener noreferrer">anatomy.id</a>, draftId: <a href="http://draft.id" target="_blank" rel="noopener noreferrer">draft.id</a> }; });</p><p>db.transaction(...) 开启一个事务，生成 tx。然后：</p><ul><li>anatomiesDao.createWithTx(tx, ...) 用 tx 插入 anatomies</li><li>draftsDao.createWithTx(tx, ...) 用同一个 tx 插入 drafts</li></ul><p>如果中途任何一步报错，Drizzle 会自动回滚整个事务，两张表都不会留下数据。</p><hr><p>如果不用 createWithTx 会怎样？</p><p>假设你错误地混用了 db 和 tx：</p><p>return db.transaction(async (tx) =&gt; { const anatomy = await anatomiesDao.create({ id: crypto.randomUUID() }); // ❌ 用 db，不在事务里！ const draft = await draftsDao.createWithTx(tx, { anatomyId: <a href="http://anatomy.id" target="_blank" rel="noopener noreferrer">anatomy.id</a>, ... }); return { anatomyId: <a href="http://anatomy.id" target="_blank" rel="noopener noreferrer">anatomy.id</a>, draftId: <a href="http://draft.id" target="_blank" rel="noopener noreferrer">draft.id</a> }; });</p><p>这时候：</p><ul><li>anatomy 已经立刻提交到数据库了</li><li>draft 还在事务里</li><li>如果 draft 插入失败回滚，anatomy 仍然留在表里</li></ul><p>这就破坏了原子性。</p><hr><p>项目里的固定模式</p><p>你去看 packages/db/src/dao/crates-dao.ts，它也有 create / createWithTx、update / updateWithTx、delete / deleteWithTx。这是项目里的标准写法：</p><ul><li>DAO：同时提供普通版和 WithTx 版</li><li>Repository：需要跨表原子操作时，用 db.transaction(...) 开事务，然后把 tx 传给 DAO 的 WithTx 方法</li></ul><p>所以记住一条规则：</p><p>▎ 在 db.transaction(...) 的回调里，所有数据库操作都必须用 tx，不能再用 db。</p><hr><p>我接着把 repository/index.ts 的导出加上，然后跑类型检查。你还有什么想问的随时打断我。</p>',35)])])}const d=a(o,[["render",i]]),l=JSON.parse('{"path":"/notes/obsidian/%E5%89%8D%E7%AB%AF/%E9%A1%B9%E7%9B%AE%E7%AC%94%E8%AE%B0/%E4%BB%A3%E8%BE%BE%E7%BD%97%E6%96%AF/%E6%95%B0%E6%8D%AE%E6%B5%81%E5%90%91/Anatomy%20%E7%9A%84%20DAO.html","title":"Anatomy 的 DAO","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"notes/obsidian/前端/项目笔记/代达罗斯/数据流向/Anatomy 的 DAO.md"}');export{d as comp,l as data};
