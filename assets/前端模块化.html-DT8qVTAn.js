import{_ as o,c as s,b as n,o as a}from"./app-NOeoeEVV.js";const t={};function c(d,e){return a(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="前端模块化" tabindex="-1"><a class="header-anchor" href="#前端模块化"><span>前端模块化</span></a></h1><p>在<code>vite(rollup)</code>的打包配置里有一个<code>format</code>的选项，可以指定最终打包产物的格式，默认情况下是<code>ES module</code>，但是不同的场景可能会有所区别，本文就想通过这个来讲讲前端模块化。</p><h2 id="iife" tabindex="-1"><a class="header-anchor" href="#iife"><span>IIFE</span></a></h2><p>自执行函数 &gt; 英文全称：immediately invoked function expressions</p><p><strong>适用场景</strong> 浏览器中带有<code>&amp;lt;script&amp;gt;</code>标签包裹的代码块，简单来说就是普通的业务应用都可，没啥兼容问题</p><p><strong>优点</strong></p><ul><li>函数作用域避免了全局变量的污染 iife也算是比较早期的模块化方案了，只需对外暴露一个全局变量，最知名的就是<code>jQuery</code></li></ul><h2 id="cjs" tabindex="-1"><a class="header-anchor" href="#cjs"><span>CJS</span></a></h2><p>真正的模块化解决方案，最早从Node应用开始 &gt; 全称：CommonJS</p><p><strong>关键词：module，exports，global，require</strong></p><p><strong>特点</strong></p><ul><li>模块加载<code>require</code>就是代码执行</li><li>所有代码运行在模块作用域中，不污染全局</li><li>模块可以多次加载，但是只在第一次加载运行，后面是缓存</li></ul><p><strong>适用场景</strong> Nodejs，浏览器端需要用其他打包工具支持</p><p>CommonJS (CJS) 的实现核心在于 Node.js 运行时的 <code>require()</code> 函数。它并非语言层面的特性，而是一个<strong>运行时</strong>概念，通过一系列步骤来加载和执行模块。</p><p>简单来说，<code>require()</code> 函数的工作流程可以概括为以下几个关键步骤：</p><ol><li><p><strong>路径解析 (Resolve)</strong> 首先，<code>require()</code> 会接收一个模块路径（如 <code>&#39;./module&#39;</code> 或 <code>&#39;lodash&#39;</code>），并将其解析为一个绝对的文件路径。这个过程会处理相对路径、绝对路径以及在 <code>node_modules</code> 目录中的查找。</p></li><li><p><strong>缓存检查 (Cache Check)</strong> Node.js 会维护一个 <code>require.cache</code> 对象来缓存已加载的模块。在加载前，<code>require()</code> 会先检查这个缓存。如果模块已经被加载过，它会直接返回缓存中 <code>module.exports</code> 的值，而不会重新执行模块代码。</p></li><li><p><strong>创建模块对象 (Create Module Object)</strong> 如果模块未被缓存，Node.js 会创建一个新的 <code>Module</code> 对象。这个对象包含了模块的元信息，如 <code>id</code> (绝对路径)、<code>exports</code> (初始为空对象 <code>{}</code>)、<code>loaded</code> (初始为 <code>false</code>) 等。</p></li><li><p><strong>提前放入缓存 (Cache Before Execution)</strong> 这是一个非常关键的设计。在模块代码执行<strong>之前</strong>，新创建的 <code>Module</code> 对象就会被放入 <code>require.cache</code> 中。这个机制是处理<strong>循环依赖</strong>的核心，它确保了当一个模块在加载过程中又去 <code>require</code> 另一个正在加载的模块时，能拿到一个不完整的但可用的导出对象，而不是陷入死循环。</p></li><li><p><strong>读取与包装 (Read and Wrap)</strong> Node.js 会读取模块文件的内容，然后将其包装在一个函数中。这个包装器函数为模块提供了独立的作用域，并注入了 <code>exports</code>, <code>require</code>, <code>module</code>, <code>__filename</code>, <code>__dirname</code> 等变量。其结构类似于：</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token punctuation">(</span><span class="token keyword">function</span> <span class="token punctuation">(</span><span class="token parameter">exports<span class="token punctuation">,</span> require<span class="token punctuation">,</span> module<span class="token punctuation">,</span> __filename<span class="token punctuation">,</span> __dirname</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token comment">// 模块的实际代码在这里</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>编译与执行 (Compile and Execute)</strong> 包装后的函数会被编译并立即执行。执行时，<code>this</code> 指向和传入的参数都是 <code>module.exports</code> 对象。模块代码中的所有变量和函数都在这个独立的函数作用域内，从而实现了模块的私有性。</p></li><li><p><strong>返回导出值 (Return Exports)</strong> 模块代码执行完毕后，<code>require()</code> 函数会返回 <code>module.exports</code> 对象。此时，模块的 <code>loaded</code> 属性会被标记为 <code>true</code>。</p></li></ol><h3 id="cjs-实现带来的核心特性" tabindex="-1"><a class="header-anchor" href="#cjs-实现带来的核心特性"><span>CJS 实现带来的核心特性</span></a></h3><ul><li><p><strong>同步加载</strong> <code>require()</code> 必须同步地返回 <code>module.exports</code>。这意味着在执行 <code>require(&#39;./module&#39;)</code> 时，程序会阻塞，直到该模块被完全加载和执行完毕。这也是为什么 CJS 不支持顶层 <code>await</code>。</p></li><li><p><strong>值的拷贝</strong> <code>require()</code> 返回的是 <code>module.exports</code> 的一个<strong>值拷贝</strong>。</p><ul><li>对于<strong>原始类型</strong>（如 number, string），传递的是值的拷贝。</li><li>对于<strong>对象类型</strong>（如 object, array），传递的是引用的拷贝（即浅拷贝，指向同一个对象）。</li></ul><p>这意味着，如果一个模块导出了一个原始类型的值，后续模块即使修改了自己作用域内的变量，也不会影响 <code>require</code> 进来的值。但如果导出的是一个对象，修改这个对象的属性，所有引用这个对象的地方都会看到变化。</p></li><li><p><strong>循环依赖处理</strong> 得益于“提前放入缓存”的机制，CJS 能够处理循环依赖。当一个模块A加载模块B，而模块B又反过来加载模块A时，B拿到的是A尚未执行完成的 <code>module.exports</code> 对象。这可能导致B只能访问到A的部分导出内容。</p></li></ul><h2 id="amd" tabindex="-1"><a class="header-anchor" href="#amd"><span>AMD</span></a></h2><p>异步模块定义</p><p>&gt; 全拼：Asynchronous module definition</p><p>amd可以理解是CommonJs在浏览器端的解决方案，cjs在服务端是<strong>同步加载</strong>依赖代码的，因为服务端都是本地磁盘读取文件，没有网络开销，速度很快，浏览器端要是也这么干，很有可能因为依赖在远程，因网络时间开销而导致出现“假死”，所以浏览器端采用<strong>异步加载</strong>模块的方式。</p><p><strong>关键词</strong>：require，define</p><p>require的第一个参数是需要异步加载的依赖列表，第二个是模块异步加载后的执行代码，define第一个参数是模块的依赖模块，第二个是模块代码</p><p><strong>优点</strong></p><ul><li>体积更小，代码<strong>按需加载</strong></li><li>不堵塞js线程运行</li></ul><h2 id="umd" tabindex="-1"><a class="header-anchor" href="#umd"><span>UMD</span></a></h2><p>联合模块定义</p><p>&gt; 全拼：Universal Module Definition</p><p>准确来说并不是一个独立的模块标准，而是集合了cjs，amd，iife等一体的打包模式，会自动判断当前可用环境。</p><p><strong>优点</strong>：</p><ul><li>一套代码，多端使用，模块化兼容性好</li></ul><p>一般会在webpack中成为备用模块</p><h2 id="esm" tabindex="-1"><a class="header-anchor" href="#esm"><span>ESM</span></a></h2><p>ES6标准中的模块规则，一统浏览器和服务端标准的解决方案</p><p>&gt; 全拼：ES6 Module</p><p><strong>关键字</strong>：import，export</p><p><strong>适用场景</strong>： 结合其他打包工具（webpack）使用或浏览器<code>&amp;lt;script type=&quot;module&quot;&amp;gt;</code>标签包裹</p><p><strong>特点</strong>：</p><ul><li>浏览器端会异步加载，延续AMD的优势，但利用了浏览器原生的解析能力，代码体积更小</li><li>模块内自动采用<strong>严格模式</strong></li><li>模块中的this指向并不是window或global，而是<code>undefined</code></li></ul><p>和cjs的三个差异：</p><ul><li>cjs 模块输出的是一个==值的拷贝（浅拷贝）==，esm输出的是==值的引用==。</li><li>cjs 模块是运行时加载，esm是编译时输出接口。</li><li>cjs==同步==加载模块，esm==异步==加载，有一个独立的模块依赖的解析阶段。</li></ul><p>在cjs中，如果多个地方都用了同一个模块，对这个模块进行修改，其实不会有影响，但是在esm中，如果你用incCounter修改了counter值，其他业务模块调用counter的时候就会有变化</p><p>原因是esm中模块加载的时候还没完全去取值，实际业务代码执行到了才会去拿真正的值，import的时候只是建了个具体地址，留着后面调用时候方便。</p>`,44)])])}const p=o(t,[["render",c]]),i=JSON.parse('{"path":"/notes/obsidian/%E5%89%8D%E7%AB%AF/%E4%B8%89%E4%BB%B6%E5%A5%97/JS/%E5%89%8D%E7%AB%AF%E6%A8%A1%E5%9D%97%E5%8C%96.html","title":"前端模块化","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"notes/obsidian/前端/三件套/JS/前端模块化.md"}');export{p as comp,i as data};
