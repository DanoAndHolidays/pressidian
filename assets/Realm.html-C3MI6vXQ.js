import{_ as a,c as s,b as e,o}from"./app-D79r4NlB.js";const t={};function p(c,n){return o(),s("div",null,[...n[0]||(n[0]=[e(`<p>在 JavaScript 中，使用 <code>instanceof Error</code> 进行跨环境（如跨 iframe、跨 Web Worker 或 Node.js 的 vm 模块）判断时失效，根本原因在于 <strong>JavaScript 的多全局执行环境（Realm）隔离机制</strong>。</p><h3 id="核心原因-每个-realm-都有独立的构造函数" tabindex="-1"><a class="header-anchor" href="#核心原因-每个-realm-都有独立的构造函数"><span>核心原因：每个 Realm 都有独立的构造函数</span></a></h3><p>在 JavaScript 规范中，每一个独立的全局执行环境（Realm）都拥有自己的一套全局对象和内置构造函数（如 <code>Array</code>、<code>Date</code>、<code>Error</code> 等）。</p><p><code>instanceof</code> 运算符的底层工作原理是：<strong>检查左侧对象的原型链（prototype chain）中，是否包含右侧构造函数的 <code>prototype</code> 属性</strong>。</p><p>当发生跨环境传递时，虽然两个环境中的构造函数名字都叫 <code>Error</code>，但它们实际上是<strong>两个完全不同的函数对象</strong>，它们的 <code>prototype</code> 属性也互不相等。因此，当你在主窗口中用主窗口的 <code>Error.prototype</code> 去匹配 iframe 中创建的 Error 对象的原型链时，自然无法匹配成功，最终返回 <code>false</code>。</p><h3 id="具体代码示例" tabindex="-1"><a class="header-anchor" href="#具体代码示例"><span>具体代码示例</span></a></h3><p>假设我们在一个主页面中嵌入了一个 iframe，并在 iframe 中抛出了一个错误，然后传递给主窗口进行判断：</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token comment">// 1. 在主窗口中创建一个 iframe</span></span>
<span class="line"><span class="token keyword">const</span> iframe <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">createElement</span><span class="token punctuation">(</span><span class="token string">&#39;iframe&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">document<span class="token punctuation">.</span>body<span class="token punctuation">.</span><span class="token function">appendChild</span><span class="token punctuation">(</span>iframe<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 2. 在 iframe 的执行环境中创建一个 Error 对象</span></span>
<span class="line"><span class="token keyword">const</span> iframeError <span class="token operator">=</span> iframe<span class="token punctuation">.</span>contentWindow<span class="token punctuation">.</span><span class="token function">Error</span><span class="token punctuation">(</span><span class="token string">&#39;This error comes from an iframe&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 3. 在主窗口中进行类型判断</span></span>
<span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>iframeError <span class="token keyword">instanceof</span> <span class="token class-name">Error</span><span class="token punctuation">)</span><span class="token punctuation">;</span>       <span class="token comment">// 输出: false ❌ (翻车)</span></span>
<span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>iframeError <span class="token keyword">instanceof</span> <span class="token class-name">iframe<span class="token punctuation">.</span>contentWindow<span class="token punctuation">.</span>Error</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出: true ✅</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>原理解析：</strong></p><ul><li><code>iframeError</code> 的原型链指向的是 <code>iframe.contentWindow.Error.prototype</code>。</li><li>主窗口中的 <code>Error</code> 是主窗口的构造函数，其 <code>prototype</code> 是 <code>window.Error.prototype</code>。</li><li>因为 <code>iframe.contentWindow.Error.prototype !== window.Error.prototype</code>，所以 <code>instanceof</code> 沿着原型链查找时找不到匹配项，返回了 <code>false</code>。</li></ul><h3 id="更好的替代方案" tabindex="-1"><a class="header-anchor" href="#更好的替代方案"><span>更好的替代方案</span></a></h3><p>为了解决这种跨环境的类型检测问题，现代 JavaScript 提供了更可靠的方案：</p><ol><li><p><strong>使用 <code>Error.isError()</code>（最新提案/规范）</strong> 这是目前最推荐的跨环境 Error 检测方法，类似于 <code>Array.isArray()</code>，它不依赖原型链，而是通过内部机制判断。</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>Error<span class="token punctuation">.</span><span class="token function">isError</span><span class="token punctuation">(</span>iframeError<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出: true ✅ (完美解决)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><p><strong>使用 <code>Object.prototype.toString.call()</code></strong> 这是一种经典的、跨环境安全的类型检测方式，通过读取对象内部的 \`\` 标签来判断。</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token class-name">Object</span><span class="token punctuation">.</span>prototype<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">.</span><span class="token function">call</span><span class="token punctuation">(</span>iframeError<span class="token punctuation">)</span> <span class="token operator">===</span> <span class="token string">&#39;[object Error]&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出: true ✅</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li></ol><p>总结来说，<code>instanceof</code> 并非有 Bug，而是它被设计为严格依赖“当前执行环境的原型链拓扑结构”。在进行跨环境通信时，应当避免使用 <code>instanceof</code>，转而使用上述更稳健的类型检测手段。</p><hr><p>你实际项目中是遇到了跨 iframe 还是跨 Worker 的场景？可以帮你针对性地写一份跨环境错误传递与校验的完整示例。</p><p>你理解得非常准确，<code>instanceof</code> 确实无法跨 iframe 或 realm 进行准确判断。关于你提到的这两个概念，它们其实是“整体”与“局部”的关系：</p><p><strong>Realm（领域/环境）</strong> 是 JavaScript 语言层面的一个概念，而 <strong>iframe</strong> 是浏览器中最典型、最常见的 Realm 体现。</p><h3 id="🌐-什么是-realm" tabindex="-1"><a class="header-anchor" href="#🌐-什么是-realm"><span>🌐 什么是 Realm？</span></a></h3><p>你可以把 Realm 想象成一个<strong>完全独立的 JavaScript 运行国度</strong>。每一个 Realm 都拥有自己独立的一套“基础设施”，包括：</p><ul><li><strong>独立的全局对象</strong>：比如浏览器中的 <code>window</code> 或通用的 <code>globalThis</code>。</li><li><strong>独立的内置对象</strong>：比如 <code>Array</code>、<code>Object</code>、<code>Function</code>、<code>Promise</code> 等。</li><li><strong>独立的执行上下文</strong>：包含各自的变量环境和词法环境。</li></ul><p>在同一个 Realm 里，<code>Array</code> 就是那个唯一的数组构造函数。但在不同的 Realm 里，虽然都叫 <code>Array</code>，但它们其实是<strong>完全不同的两个函数对象</strong>，内存地址和引用都不一样。</p><h3 id="🖼️-什么是-iframe" tabindex="-1"><a class="header-anchor" href="#🖼️-什么是-iframe"><span>🖼️ 什么是 iframe？</span></a></h3><p>在浏览器中，<strong>每一个 <code>&amp;lt;iframe&amp;gt;</code> 标签都会创建出一个全新的、独立的 Realm</strong>。</p><ul><li>主页面是一个 Realm，拥有自己的 <code>window</code> 和 <code>Array</code>。</li><li>嵌入的 iframe 是另一个 Realm，它也拥有自己独立的 <code>window</code> 和 <code>Array</code>。</li></ul><h3 id="💡-为什么会导致-instanceof-失效" tabindex="-1"><a class="header-anchor" href="#💡-为什么会导致-instanceof-失效"><span>💡 为什么会导致 instanceof 失效？</span></a></h3><p><code>instanceof</code> 的工作原理是检查“构造函数的 <code>prototype</code> 是否存在于对象的原型链上”。</p><p>当你把 iframe 里的一个数组传递到主页面时：</p><ol><li>这个数组的原型链上，挂载的是 <strong>iframe 那个 Realm</strong> 里的 <code>Array.prototype</code>。</li><li>当你在主页面使用 <code>instanceof Array</code> 时，你用的是 <strong>主页面 Realm</strong> 的 <code>Array</code> 构造函数。</li><li>因为这两个 Realm 的 <code>Array</code> 根本不是同一个东西，原型链自然对不上号，所以返回 <code>false</code>。</li></ol><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token comment">// 简单演示跨 Realm 导致的 instanceof 失效</span></span>
<span class="line"><span class="token keyword">const</span> iframe <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">createElement</span><span class="token punctuation">(</span><span class="token string">&#39;iframe&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">document<span class="token punctuation">.</span>body<span class="token punctuation">.</span><span class="token function">appendChild</span><span class="token punctuation">(</span>iframe<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 获取 iframe 里的独立 Realm 的 window 对象</span></span>
<span class="line"><span class="token keyword">const</span> iframeWindow <span class="token operator">=</span> iframe<span class="token punctuation">.</span>contentWindow<span class="token punctuation">;</span></span>
<span class="line"><span class="token comment">// 在 iframe 的 Realm 里创建一个数组</span></span>
<span class="line"><span class="token keyword">const</span> arrFromIframe <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">iframeWindow<span class="token punctuation">.</span>Array</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>arrFromIframe <span class="token keyword">instanceof</span> <span class="token class-name">Array</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// false (主页面的 Array 不认识它)</span></span>
<span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>arrFromIframe <span class="token keyword">instanceof</span> <span class="token class-name">iframeWindow<span class="token punctuation">.</span>Array</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// true (自己 Realm 的 Array 认识它)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="🛠️-如何解决跨-realm-的类型判断" tabindex="-1"><a class="header-anchor" href="#🛠️-如何解决跨-realm-的类型判断"><span>🛠️ 如何解决跨 Realm 的类型判断？</span></a></h3><p>针对这种跨 iframe/realm 的场景，推荐使用以下两种更安全的方法：</p><ul><li><strong><code>Array.isArray(arr)</code></strong>：专门用于判断数组，它内部做了特殊处理，可以完美识别跨 Realm 的数组。</li><li><strong><code>Object.prototype.toString.call(obj)</code></strong>：通用的类型判断“金标准”，例如 <code>Object.prototype.toString.call(arrFromIframe) === &#39;[object Array]&#39;</code>，它不受 Realm 隔离的影响。</li></ul><p>在 JavaScript 规范中，<strong>Realm（域）</strong> 是一个核心的底层概念。简单来说，它是一个<strong>完全独立的 JavaScript 执行环境（或称为“独立的宇宙”）</strong>。</p><p>我们之前讨论的跨 <code>iframe</code> 或跨 <code>Worker</code> 导致 <code>instanceof</code> 失效的问题，其根本原因就在于它们处于不同的 Realm 中。</p><h3 id="_1-realm-的核心组成" tabindex="-1"><a class="header-anchor" href="#_1-realm-的核心组成"><span>1. Realm 的核心组成</span></a></h3><p>每一个 Realm 都拥有自己专属的“基础设施”，互不干扰。根据 ECMAScript 规范，一个 Realm 主要由以下部分组成：</p><ul><li><strong>全局对象（Global Object）</strong>：例如浏览器环境中的 <code>window</code> 或 <code>globalThis</code>。</li><li><strong>内置对象（Intrinsic Objects）</strong>：一套与该全局对象绑定的基础构造函数和原型，如 <code>Object</code>、<code>Array</code>、<code>Error</code>、<code>Promise</code> 等。</li><li><strong>全局词法环境（Global Environment）</strong>：用于解析全局变量和标识符的作用域。</li><li><strong>执行上下文（Execution Context）</strong>：代码运行时的状态追踪（包括调用栈、<code>this</code> 绑定等）。</li></ul><p><strong>关键点在于：</strong> 即使两个 Realm 中的 <code>Array</code> 构造器功能完全一样，它们在内存中也是<strong>两个完全不同的函数对象</strong>，拥有各自独立的 <code>prototype</code>。</p><h3 id="_2-浏览器中的-realm-实例" tabindex="-1"><a class="header-anchor" href="#_2-浏览器中的-realm-实例"><span>2. 浏览器中的 Realm 实例</span></a></h3><p>在浏览器环境中，Realm 与全局对象是一一对应的。以下场景都会创建独立的 Realm：</p><ul><li><strong>主页面（Top Main Realm）</strong>：网页默认运行的 Realm。</li><li><strong>iframe</strong>：每一个 <code>&amp;lt;iframe&amp;gt;</code> 都会创建一个全新的 Realm，即使它与主页面同源。</li><li><strong>Web Workers</strong>：每个 Worker（包括 Dedicated、Shared、Service Worker）都有自己独立的 Realm。</li></ul><h3 id="_3-直观感受-realm-的隔离性" tabindex="-1"><a class="header-anchor" href="#_3-直观感受-realm-的隔离性"><span>3. 直观感受 Realm 的隔离性</span></a></h3><p>我们可以通过以下代码直观地看到 Realm 的隔离效果：</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js"><pre><code class="language-javascript"><span class="line"><span class="token comment">// 1. 创建一个 iframe</span></span>
<span class="line"><span class="token keyword">const</span> iframe <span class="token operator">=</span> document<span class="token punctuation">.</span><span class="token function">createElement</span><span class="token punctuation">(</span><span class="token string">&#39;iframe&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">document<span class="token punctuation">.</span>body<span class="token punctuation">.</span><span class="token function">appendChild</span><span class="token punctuation">(</span>iframe<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 2. 比较两个 Realm 的全局对象</span></span>
<span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>window <span class="token operator">===</span> iframe<span class="token punctuation">.</span>contentWindow<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出: false</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 3. 比较两个 Realm 的内置构造函数</span></span>
<span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>window<span class="token punctuation">.</span>Array <span class="token operator">===</span> iframe<span class="token punctuation">.</span>contentWindow<span class="token punctuation">.</span>Array<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出: false</span></span>
<span class="line">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>window<span class="token punctuation">.</span>Error <span class="token operator">===</span> iframe<span class="token punctuation">.</span>contentWindow<span class="token punctuation">.</span>Error<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出: false</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这就是为什么跨 Realm 传递对象时，<code>instanceof</code> 会返回 <code>false</code> 的根本原因。</p><h3 id="_4-为什么需要-realm-实际应用场景" tabindex="-1"><a class="header-anchor" href="#_4-为什么需要-realm-实际应用场景"><span>4. 为什么需要 Realm？（实际应用场景）</span></a></h3><p>Realm 的存在不仅仅是为了解释 <code>instanceof</code> 的坑，它在现代前端工程中有着极其重要的安全与架构价值：</p><ul><li><strong>安全沙箱（Sandbox）</strong>：在插件系统、在线代码编辑器（如 JSFiddle、CodePen）中，运行第三方或用户编写的不可信代码时，将其放入独立的 Realm。这样即使恶意代码重写了 <code>Array.prototype.push</code> 或 <code>Object.prototype</code>，也只会污染它自己的 Realm，不会导致主应用崩溃。</li><li><strong>微前端架构</strong>：在微前端中，多个子应用可能运行在同一个页面上。通过为每个子应用分配独立的 Realm，可以有效隔离全局变量和原型链，避免子应用之间的冲突。</li><li><strong>测试环境隔离</strong>：在单元测试中，每次运行测试用例前可以创建一个全新的 Realm，执行完毕后销毁，从而保证测试环境的绝对干净，无需刷新整个页面。</li></ul><h3 id="_5-未来的发展-shadowrealm-api" tabindex="-1"><a class="header-anchor" href="#_5-未来的发展-shadowrealm-api"><span>5. 未来的发展：ShadowRealm API</span></a></h3><p>在过去，开发者只能通过 <code>iframe</code> 或 <code>Web Worker</code> 来间接获取 Realm，这带来了沉重的 DOM 开销或通信成本。</p><p>目前，TC39 正在推进 <strong>ShadowRealm API 提案</strong>（目前处于 Stage 3 阶段）。它允许开发者在 JavaScript 层面直接通过 <code>new ShadowRealm()</code> 创建一个轻量级、完全隔离的 Realm，并通过 <code>evaluate()</code> 方法安全地执行字符串代码，而无需创建 iframe。这将是未来实现安全沙箱和代码隔离的终极标准方案。</p><hr><p>需要我写一份跨 Realm 安全通信的完整示例吗？比如用 postMessage 传递错误对象，并在接收端用 Error.isError 正确校验。</p>`,54)])])}const r=a(t,[["render",p]]),i=JSON.parse('{"path":"/notes/obsidian/%E5%89%8D%E7%AB%AF/%E9%9D%A2%E8%AF%95/%E5%85%AB%E8%82%A1/JS/Realm.html","title":"","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"notes/obsidian/前端/面试/八股/JS/Realm.md"}');export{r as comp,i as data};
