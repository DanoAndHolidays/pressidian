import{_ as e,c as t,b as r,o as n}from"./app-B-Oysd-8.js";const s={};function l(a,o){return n(),t("div",null,[...o[0]||(o[0]=[r('<h1 id="_2线上稳定-分页导出" tabindex="-1"><a class="header-anchor" href="#_2线上稳定-分页导出"><span>2线上稳定&amp;分页导出</span></a></h1><h3 id="你修复了4个高优bug-挑选两个典型bug说明排查思路、根因与修复方案" tabindex="-1"><a class="header-anchor" href="#你修复了4个高优bug-挑选两个典型bug说明排查思路、根因与修复方案"><span>你修复了4个高优Bug，挑选两个典型Bug说明排查思路、根因与修复方案</span></a></h3><p>这几个bug，有两个是我自己实现的时候没有这方面的知识导致的，另一个是后端数据</p><p><strong>数据渲染不一致</strong>：一个表单，需求是添加一个默认值，有不同的入口，其中一个入口在一个具体的业务，用户点进来，就是想要去配置当前的这个业务，这里呢就添加了当前业务的一些信息，不需要用户去填。有一个必填的字段，自动添加后，视图发生了变化，但是发送给后端的数据没有变化，依旧是空着的，就会报一个错。用户就比较着急，实际上请求根本就没发出去</p><p>“当时遇到了一个表单数据不一致的Bug。因为入口带有特定的业务参数，我们需要在页面加载时拉取数据并回填到一个必填项中。我当时只关注了 UI 层面的渲染，给 Input 绑定了 value 却遗漏了 onChange（或者在 Antd 中误用了底层控件的 defaultValue 而不是表单的 initialValue）。导致视图上看着有值，但实际提交时表单收集到的状态依然是空的。后来我深入理解了 React 受控组件的双向绑定原理（以及 Antd Form 的状态托管机制），通过补全状态同步逻辑（或改用 setFieldsValue / initialValue）修复了这个问题。”</p><p>导致了视图与状态的分离</p><ol><li><strong>视觉欺骗（View 更新了）</strong>：当 <code>useEffect</code> 里的接口返回数据后，<code>setFormData</code> 触发了组件重新渲染。此时 <code>&amp;lt;input value={formData.requiredField} /&amp;gt;</code> 确实拿到了新值，所以用户在屏幕上看到了默认值被成功填入。</li><li><strong>状态断裂（State 没更新）</strong>：因为这个 <code>&amp;lt;input&amp;gt;</code> 绑定了 <code>value</code>，它变成了一个<strong>受控组件</strong>。但在上面的代码中，<strong>缺少了 <code>onChange</code> 处理函数</strong>。这意味着，虽然初始值被强行塞进去了，但表单内部并没有建立起双向的数据同步机制。在某些极端情况或框架封装下，如果没有正确的 <code>onChange</code> 回调去更新 State，后续提交时拿到的可能还是旧的空状态。</li></ol><p><strong>审批流状态异常</strong>：前端展示状态与Url中的查询参数没有一一对应。“当时遇到了一个审批流状态异常的Bug。由于我们在单页应用中使用了 useState 来接收路由的查询参数，但忽略了 useState 仅在组件首次挂载时初始化的特性。当用户在同一个审批详情页内切换不同单据时，URL 虽然更新了，但组件被复用，本地 State 并没有随之刷新，导致视图展示的还是旧数据。</p><p>后来我重构了这部分逻辑，明确了‘<strong>URL 是 Web 应用的唯一真理来源</strong>’这一原则。</p><ul><li>要么通过 useEffect 严格监听 URL 参数变化来同步 State</li><li>要么直接废弃冗余的 State，改用 React Router 提供的 useSearchParams 实现 URL 与 UI 的双向实时同步。”</li></ul><p><strong>导出失败</strong>：是因为接口升级，由于这个业务25年刚有，就只有一年的数据，所以导出的时候就只需要导出一年的就要可以了，后来我们升级了接口，这里导出，自测没有测到</p><p><strong>脏数据</strong>：最后一个bug就是业务升级的时候，之前没有用的字段也要展示出来，就发现了这个字段的数据与当前业务并不对应，原因是后端存有脏数据</p><h3 id="修复-数据渲染不一致-这个偶现bug。这类bug最难复现-能还原一下你完整的排查思路吗" tabindex="-1"><a class="header-anchor" href="#修复-数据渲染不一致-这个偶现bug。这类bug最难复现-能还原一下你完整的排查思路吗"><span>修复“数据渲染不一致”这个偶现Bug。这类Bug最难复现，能还原一下你完整的排查思路吗？</span></a></h3><ul><li><strong>标准话术</strong>： <ol><li><strong>缩小范围</strong>：我先放弃了“复现”这个念头，因为它发生概率只有5%。我转而从<strong>日志</strong>入手。在监控平台上，我筛选出所有遇到过这个Bug的用户操作轨迹。我发现，所有出错的用户，都在<strong>极短时间内（1秒内）连续触发了3次以上筛选条件变更</strong>。</li><li><strong>构造假设</strong>：我立刻想到可能是<strong>请求竞态</strong>问题。后端接口响应时间不固定，如果先发出的请求后返回，它携带的旧筛选条件就会覆盖后发出的、携带新筛选条件的请求所更新的界面。</li><li><strong>代码验证</strong>：我审查代码，发现请求函数确实没有做任何竞态处理。<code>axios</code>实例没有<code>cancelToken</code>，组件的<code>useEffect</code>也没有<code>cleanup</code>函数去取消未完成的请求。</li><li><strong>分步修复</strong>：我的修复方案不是一步到位，而是<strong>分层加固</strong>： <ul><li><strong>第一层（立即生效）</strong>：在<code>useEffect</code>的<code>cleanup</code>函数中，使用<code>AbortController</code>取消上一次请求。</li><li><strong>第二层（用户体验）</strong>：添加全局<code>isFetching</code>状态，在请求期间禁用所有筛选和刷新按钮，从根源上杜绝用户快速连点。</li><li><strong>第三层（终极兜底）</strong>：给每个请求增加一个递增的<code>requestId</code>，在<code>setData</code>时只更新最新<code>requestId</code>的请求结果。</li></ul></li></ol></li></ul><hr><h3 id="若线上出现导出文件与后端原始数据条数不一致-p1故障-完整的止损、排查、修复、复盘流程是什么" tabindex="-1"><a class="header-anchor" href="#若线上出现导出文件与后端原始数据条数不一致-p1故障-完整的止损、排查、修复、复盘流程是什么"><span>若线上出现导出文件与后端原始数据条数不一致（P1故障），完整的止损、排查、修复、复盘流程是什么？</span></a></h3><p><strong>回答</strong>：财务数据不一致属于高优先级故障，我会按五步处理：</p><ol><li><strong>紧急止损</strong>：临时下线导出功能，同步财务、运营团队，暂停结算流程；</li><li><strong>分层排查</strong>：依次核对接口数据、前端分页逻辑、数据清洗规则、浏览器兼容性，定位问题根源；</li><li><strong>快速修复</strong>：本地复现问题，编写修复代码，预发环境完成全量对账后灰度上线；</li><li><strong>全量验证</strong>：抽取多组样本，人工比对文件与原始数据，确认无误；</li><li><strong>复盘优化</strong>：代码层增加条数校验断言，监控层新增异常埋点，流程层补充对账测试用例，输出复盘文档。</li></ol><hr><h3 id="该模块对接财务结算-数据零容错-你做了哪些校验与兜底策略保证数据准确" tabindex="-1"><a class="header-anchor" href="#该模块对接财务结算-数据零容错-你做了哪些校验与兜底策略保证数据准确"><span>该模块对接财务结算，数据零容错，你做了哪些校验与兜底策略保证数据准确？</span></a></h3><p>财务场景零容错，我主要从<strong>数据校验、边界兜底、日志留痕</strong>三个维度做了保障： 5. <strong>入参&amp;出参双重校验</strong>：所有传给后端的筛选、导出参数，前端做格式、范围校验，拦截非法参数；接口返回的数据，校验必填字段、数值范围，异常数据直接标记并在页面提示，不渲染错误数据。 6. <strong>导出数据校验</strong>：分页导出时，每一页数据都做条数校验，对比接口返回条数和实际解析条数，不一致则立即终止导出并提示异常；最终合并文件后，统计总条数展示给用户，方便核对。 7. <strong>状态幂等处理</strong>：审批、导出这类操作，防止用户重复点击。我增加按钮防抖+加载锁，操作中禁用按钮，避免重复发起请求造成重复数据、状态错乱。 8. <strong>错误日志埋点</strong>：数据异常、导出失败时，自动上报错误日志，包含用户ID、操作时间、请求参数，方便后续快速溯源。</p><hr><h3 id="数据渲染不一致属于线上偶现幽灵bug-本地无法复现-完整讲下排查流程、隐性诱因与分层修复方案" tabindex="-1"><a class="header-anchor" href="#数据渲染不一致属于线上偶现幽灵bug-本地无法复现-完整讲下排查流程、隐性诱因与分层修复方案"><span>数据渲染不一致属于线上偶现幽灵Bug，本地无法复现，完整讲下排查流程、隐性诱因与分层修复方案</span></a></h3><p><strong>回答</strong>：该Bug线上偶发、本地无法复现，排查初期我先怀疑后端接口和浏览器缓存，逐一排除后，借助监控平台分析用户操作时序，发现异常集中在<strong>快速切换筛选条件</strong>的场景。 根因分为显性和隐性两层：显性是请求竞态，旧请求覆盖新数据；隐性是原代码无请求取消逻辑、无数据版本标记，放大了问题。 我做了三层兜底修复：第一，使用<code>AbortController</code>终止过期请求；第二，增加全局请求锁，从操作端规避并发；第三，为每个请求设置<code>requestId</code>，仅渲染最新请求的数据。三层方案落地后，该Bug彻底解决。</p><hr><h3 id="前端分页导出是网上通用方案-你只是照搬代码-没有自己的思考-怎么回应" tabindex="-1"><a class="header-anchor" href="#前端分页导出是网上通用方案-你只是照搬代码-没有自己的思考-怎么回应"><span>前端分页导出是网上通用方案，你只是照搬代码，没有自己的思考，怎么回应？</span></a></h3><p><strong>回答</strong>：我参考了通用实现思路，但结合财务零容错的业务场景做了大量定制改造。通用方案仅实现基础导出功能，而我额外完成了编码兼容、特殊字符转义、断点续传、数据对账、多层请求防护、内存释放等专属兜底逻辑。同时我对比了四种导出方案的边界与优劣，结合当前数据量级、排期成本选定最优解。所有边界处理、风险防控逻辑都是结合业务独立设计，并非直接照搬。</p><hr><h3 id="前端分页具体实现" tabindex="-1"><a class="header-anchor" href="#前端分页具体实现"><span>前端分页具体实现</span></a></h3><p><a href="http://localhost:5173/yike/src-react/#/tree-demo" target="_blank" rel="noopener noreferrer">http://localhost:5173/yike/src-react/#/tree-demo</a> 大数据量导出时浏览器卡顿，你引入了<strong>前端分页导出</strong>方案，请说明原有方案的问题根因、你的实现逻辑，以及优化后的效果？ <strong>满分答案：</strong> 原有导出逻辑是：前端一次性请求<strong>全量大数据</strong>，再在前端组装文件、触发下载。当数据量达到上万条时，会出现两个问题：一是单次接口返回数据体积过大，内存占用飙升；二是前端一次性解析、拼接表格文件，主线程被阻塞，页面卡死、浏览器卡顿，甚至导出失败。同时这个模块对接财务结算，导出失败会直接影响业务流程。</p><p>我落地的<strong>前端分页导出</strong>整体逻辑：</p><ol><li><strong>分页请求</strong>：不再一次性拉取全量数据，前端设置固定分页大小，循环分页调用接口，分批获取数据；</li><li><strong>分片组装</strong>：每拿到一页数据，就实时拼接文件内容，不把所有数据存在内存中，控制内存占用；</li><li><strong>进度反馈</strong>：增加导出进度条，让用户感知当前状态，避免误以为页面卡死；</li><li><strong>异常中断</strong>：增加中断逻辑，网络失败、用户手动取消时，终止分页请求，清空已缓存数据。</li></ol><h3 id="大数据量导出出现浏览器卡顿-介绍原有问题、前端分页导出实现逻辑与优化效果" tabindex="-1"><a class="header-anchor" href="#大数据量导出出现浏览器卡顿-介绍原有问题、前端分页导出实现逻辑与优化效果"><span>大数据量导出出现浏览器卡顿，介绍原有问题、前端分页导出实现逻辑与优化效果</span></a></h3><p>卡有两个原因，一是业务升级导致了数据量的增长2-3w，PM希望保留全量展示的形式，在没导出前就已经很卡了。这里改成了[虚拟滚动 ⌚️](../新简历实习/虚拟滚动 <a href="http://xn--dih7537f.md" target="_blank" rel="noopener noreferrer">⌚️.md</a>)</p><p>“引入前端分页导出方案解决大数据量卡顿”，能详细讲讲具体实现吗？前端导出如果用户关闭了页面怎么办？如何处理“脏数据”？</p><ul><li><strong>标准话术</strong>： <ol><li><strong>具体实现</strong>：我的方案不是简单的循环请求。首先，获取总条数，计算出总页数。然后，我实现了一个<strong>并发队列控制器</strong>，设置最大并发数为3，分批请求所有分页数据。这个控制器可以避免瞬时大量请求压垮后端，也防止浏览器崩溃。每获取一页数据，就立即通过<code>Blob</code>和<code>URL.createObjectURL</code><strong>实时增量写入</strong>到文件流中，而不是等所有数据都到齐了再生成文件，从而控制内存峰值。</li><li><strong>页面关闭处理</strong>：这确实是一个关键问题。我增加了两个保护：第一，在请求队列中使用<code>AbortController</code>，监听页面的<code>beforeunload</code>事件，当用户关闭页面时，主动中止所有未完成的请求。第二，利用<code>localStorage</code>存储导出任务状态（如“导出中”、“已完成”），如果用户意外关闭页面，下次进入时页面会检测到未完成的任务，并提示“上次导出未完成，是否继续？”。</li><li><strong>“脏数据”校验</strong>：财务数据最怕出错。我在导出过程中增加了<strong>数据对账校验</strong>。每获取一页数据，我都对比后端返回的<code>count</code>字段和我实际接收到的数组长度。如果两者不符，立即终止整个导出流程，并上报错误日志。最后，我还会将总导出条数明确展示给用户，让用户二次确认，为财务审计提供凭证。</li></ol></li></ul><p><strong>注意</strong>：面试官问过“分片下载”，你要清晰区分。这里深入分页导出的所有细节。</p><p><strong>面试官可能问：</strong></p><ul><li>为什么不用后端一次性导出？</li><li>前端分页导出时，如何控制并发请求数？如果某一页请求失败怎么办？</li><li>你怎么合并多个请求返回的数据？会不会导致内存溢出？</li><li>导出的文件格式是什么？如何生成下载？</li><li>导出过程中用户关闭页面怎么办？</li></ul><p><strong>追问：如果某页请求失败重试仍然失败，你希望至少导出成功的数据，怎么做？</strong> &gt; 我们可以实现“部分导出”：记录失败页索引，继续导出其他页，最后生成一个错误报告，提示用户“第 3、7 页导出失败，已导出其余 98 页”。用户可手动补充导出失败部分。</p><hr><h3 id="分片下载-vs-分页下载" tabindex="-1"><a class="header-anchor" href="#分片下载-vs-分页下载"><span>分片下载 vs 分页下载</span></a></h3><p>你在简历中写的是 <strong>“引入前端分页导出方案，解决大数据量下浏览器卡顿问题”</strong>，但面试官问的是 <strong>“分片下载”</strong>。说明他可能故意混淆，或者想考察你对<strong>大文件/大数据量处理</strong>的深度理解。</p><p><strong>1. 明确区分：我们做的是“分页导出”，不是“分片下载”</strong></p><ul><li><strong>分页导出</strong>：前端把大数据拆成多个小请求，每次请求一部分数据（比如每页 5000 条），拿到后边合并边生成文件，避免一次性请求 10 万条导致内存暴涨或浏览器假死。</li><li><strong>分片下载</strong>：一般是后端支持 HTTP Range，前端并发请求多个字节范围，最后用 <code>Blob</code> 拼接，用于大文件（视频、安装包）或断点续传。</li></ul><p><strong>2. 我的具体实现（分页导出）</strong><br> 在 Quota 配额管理模块，用户导出时可能涉及数万条记录。我做了：</p><ol><li>先调接口获取总条数，计算总页数（每页 5000 条）</li><li>使用 <code>Promise.all</code> 或循环 + <code>async/await</code> 控制并发数（比如同时发 3 页请求）</li><li>将每页返回的 JSON 数据依次推入一个数组，最后用 <code>sheetjs</code> 或 <code>Blob</code> + <code>URL.createObjectURL</code> 生成 Excel/CSV</li><li>添加 loading 进度提示（已完成 x / 总页数）</li></ol><p>“为什么不用分片下载”</p><p>因为我们的数据是结构化 JSON，而不是二进制大文件。分片下载需要后端支持 <code>Content-Range</code>，而我们的导出接口是 REST 风格的分页接口，所以前端分页是更合适的方案。<br> 如果是音视频或大文件，我会考虑使用 <code>fetch</code> 的 <code>Range</code> 头 + <code>AbortController</code> 实现可暂停的分片下载。</p><hr><h3 id="前端分页导出用到了哪些web-api-遇到中文乱码、文件损坏、内存溢出、页面休眠中断如何解决-对比四种导出方案的优劣与选型理由" tabindex="-1"><a class="header-anchor" href="#前端分页导出用到了哪些web-api-遇到中文乱码、文件损坏、内存溢出、页面休眠中断如何解决-对比四种导出方案的优劣与选型理由"><span>前端分页导出用到了哪些Web API？遇到中文乱码、文件损坏、内存溢出、页面休眠中断如何解决？对比四种导出方案的优劣与选型理由</span></a></h3><p><strong>回答</strong>：核心基于<code>Fetch</code>、<code>Blob</code>、<code>URL.createObjectURL</code>实现。</p><ol><li><strong>中文乱码</strong>：CSV默认GBK编码，在文件头部添加<code>\\ufeff</code> BOM标记，强制UTF-8解析；</li><li><strong>文件损坏</strong>：对字段特殊字符做转义，统一换行符与分隔符，遵循CSV格式规范；</li><li><strong>内存溢出</strong>：分片拼接数据，不维护全量字符串，导出完成后调用<code>URL.revokeObjectURL</code>释放内存；</li><li><strong>页面休眠中断</strong>：监听<code>visibilitychange</code>事件，记录分页断点，页面激活后继续导出。</li></ol><p>四种方案对比：</p><ul><li>前端全量导出：实现简单，大数据易卡顿，仅适用于千条以内数据；</li><li>前端分页导出：内存可控、支持进度展示，需处理分页逻辑，适配本次1-5万条数据场景；</li><li>Web Worker导出：不阻塞主线程，但无法调用下载API，通信复杂；</li><li>服务端异步导出：前端零压力，支持十万级数据，但需要后端开发、跨团队排期长。</li></ul><p>本次选择分页导出，是因为该量级下它平衡了体验、开发成本与资源消耗，且能快速解决线上紧急问题。</p>',56)])])}const c=e(s,[["render",l]]),i=JSON.parse('{"path":"/notes/obsidian/%E5%89%8D%E7%AB%AF/%E9%9D%A2%E8%AF%95/%E9%A1%B9%E7%9B%AE/Quota_%E7%81%AB%E5%B1%B1BABI/%E6%97%A7%E7%AE%80%E5%8E%86%E5%AE%9E%E4%B9%A0/2%E7%BA%BF%E4%B8%8A%E7%A8%B3%E5%AE%9A_%E5%88%86%E9%A1%B5%E5%AF%BC%E5%87%BA.html","title":"2线上稳定&分页导出","lang":"zh-CN","frontmatter":{},"git":{},"filePathRelative":"notes/obsidian/前端/面试/项目/Quota&火山BABI/旧简历实习/2线上稳定&分页导出.md"}');export{c as comp,i as data};
